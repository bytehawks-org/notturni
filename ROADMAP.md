# Specifiche e stato di avanzamento

Questo documento è il riferimento pratico per capire **cosa prevede il progetto**,
**cosa è già stato implementato** e **cosa resta da fare**. Fa anche da workplan:
le righe segnate 🟡 e ⚪ sono, di fatto, i task aperti.

Il documento "sorgente" delle intenzioni di prodotto (visione, tono, esempi di
paragone, vincoli architetturali di partenza) resta ad uso interno del team/degli
agenti che generano il codice e non è linkato dalla documentazione pubblica:
questo file ne è la trascrizione verificabile, specifica per specifica, contro
lo stato reale del codice.

Legenda stato:

- ✅ **Implementato** — presente nel codice e coperto da test e/o verifica manuale.
- 🟡 **Parziale** — presente ma con limitazioni note, o infrastruttura pronta e non
  ancora collegata a logica applicativa.
- ⚪ **Da fare** — non ancora iniziato.

## 1. Prodotto e regole di dominio

| Specifica | Stato | Note / task residuo |
|---|---|---|
| Multiutente & multilingua, enfasi GDPR/EU | 🟡 | Multilingua implementato (vedi sotto). Export (Art. 20) e cancellazione (Art. 17) self-service dell'account ora implementati (vedi righe dedicate sotto). **Restano fuori scope**, più di prodotto/legale che tecnici: un registro dei consensi (oggi non c'è alcun consenso granulare da tracciare — solo accettazione implicita in registrazione) e un'informativa privacy strutturata oltre alla pagina statica libera già disponibile (`/p/privacy`, contenuto a discrezione dell'Amministratore). **B6**: registro/coda delle richieste GDPR con scadenza a 30 giorni (`GET /api/v1/admin/gdpr`), richieste inserite dagli admin per conto dell'utente con seconda approvazione per le cancellazioni; impostazioni di piattaforma persistite (`platform_config`: registrazione, SSO, MFA per admin, nomi riservati, soglia moderazione, blog massimi, commenti anonimi). |
| Export dei dati dell'account (GDPR Art. 20) | ✅ | `GET /api/v1/users/me/export-data` (`backend/app/domain/gdpr.py::export_user_data`): profilo, blog di proprietà, post e commenti scritti (ovunque, non solo sui propri blog), frammenti salvati, follow (in entrambe le direzioni), token API (mai i segreti/hash) ed eventi di audit di cui l'utente è l'attore (fino a 1000, i più recenti). Dashboard: pulsante "Scarica i miei dati" in `dashboard/profile`, scarica un file JSON. **Corretto in questo blocco**: include ora anche il claim di dominio custom pending/non verificato (mai il token di verifica), che restava escluso se l'export veniva scaricato prima di completare la verifica DNS. |
| Cancellazione dell'account (GDPR Art. 17) | ✅ | `DELETE /api/v1/users/me` (richiede ridigitare il proprio username come conferma) **anonimizza l'account invece di cancellare la riga `users`**: nessuna funzionalità di trasferimento/cancellazione blog esiste oggi, e un utente può essere proprietario di blog pubblici con collaboratori o aver commentato su blog altrui — cancellare la riga richiederebbe bloccare l'operazione o cancellare a cascata contenuto di terzi. Cancellati per intero: sessioni, token API, codici MFA email pendenti, identità SSO, link social, frammenti, follow, membership come collaboratore su blog altrui. Lasciati intatti (compatibile con GDPR Art. 17.3, diritti di terzi): blog di proprietà, post, commenti — l'autore compare da subito come "Utente eliminato" ovunque (stessa risoluzione dinamica degli alias, `app/domain/display_names.py`). Evento `user.account_deleted` in audit log. Dashboard: form di conferma in `dashboard/profile`. **Blog**: cancellazione con 30 giorni di tolleranza e ripristino (`DELETE /api/v1/blogs/{slug}` + `POST .../restore`), purge definitivo dal worker di manutenzione; export ZIP del blog (`GET /api/v1/blogs/{slug}/export`); pausa volontaria e trasferimento di proprietà a un coautore — vedi `backend/API.md`. |
| Massimo 5 blog per utente | ✅ | `MAX_BLOGS_PER_USER` in `backend/app/domain/blog_rules.py`. |
| Nome autore in pubblico diverso dal nome utente reale | ✅ | Scritto sulla colonna `Post.author_display_name` alla creazione/modifica del post, ma **ricalcolato ad ogni lettura** in `backend/app/api/v1/posts.py::_post_out` (stessa regola di risoluzione, `_resolve_author_display_name`): alias per-blog del collaboratore (`blog_memberships.author_display_name`) → nome pubblico predefinito del blog (`Blog.default_author_display_name`) — se uno dei due esiste è imposto — → altrimenti la preferenza di profilo `User.post_author_name_style` (`username` di default \| `full_name` \| `display_name`, condivisa con i commenti tramite `app/domain/display_names.py::resolve_personal_display_name`). Un alias cambiato dopo la pubblicazione si riflette quindi subito su **tutti** i post già scritti di quel blog, non solo su quelli risalvati dall'autore. Nessun override per singolo post. `User.display_name` è anche l'intestazione del profilo pubblico quando valorizzato. |
| Username univoco e citabile come `@username` | ✅ | Formato validato in `backend/app/domain/usernames.py` (minuscole/cifre, `-`/`_` interni, 3–32 caratteri) più blacklist. Modificabile da `PATCH /api/v1/users/me` (dashboard/profilo), **al massimo un cambio ogni 5 giorni** (`USERNAME_CHANGE_COOLDOWN_DAYS`, `users.username_changed_at`, `409` con la data del prossimo cambio consentito se violato — il primo cambio in assoluto non è mai bloccato): tutto il resto del sistema referenzia l'utente per id, non per stringa, quindi un cambio consentito si riflette subito ovunque (post, commenti, autocomplete) — **unica eccezione nota**: le `@menzioni` già scritte nel testo di post/pagine sono salvate come testo semplice e restano invariate, puntando allo username precedente (stesso limite delle vecchie URL `/u/{username}`, che smettono di risolvere). Le `@username` nel contenuto dei post vengono trasformate in link al profilo dal rendering frontend (`frontend/src/lib/markdown.ts`), con autocomplete nell'editor (`GET /api/v1/blogs/{slug}/mentionable-users`: proprietario, collaboratori, follower del blog). Disattivabile per blog con `Blog.mentions_enabled` (attivo di default). Riferimenti `[[articolo]]`/`[[blog:articolo]]` e menu `/` restano da fare — vedi `todo/EDITOR.md`. |
| Cambio email con doppia verifica (vecchia + nuova casella) | ✅ | Flusso a due passi (`POST /api/v1/users/me/email/request` → `.../verify-current` → `.../verify-new`), stesso meccanismo OTP dell'MFA email (RabbitMQ + worker `worker-email-otp`), tabella dedicata `email_change_requests` (non riusa `mfa_email_codes`, per non confondere i due tipi di codice). Nessun campo `email` in `PATCH /users/me`: solo questo flusso può cambiarla. Verificato dal vivo (mailhog): codice alla vecchia casella, poi alla nuova, login con la nuova email funzionante a fine flusso. `backend/app/domain/email_change.py`. |
| Dominio personalizzato verificato via DNS + badge di verifica | ✅ | Stile Bluesky: un dominio per utente (`custom_domains`), verificato pubblicando un record TXT (`_notturni-challenge.<dominio>`) sul proprio DNS (`app/domain/custom_domains.py`, `dnspython`, timeout 5s, fail sulla singola verifica non sulla feature). Verifica riuscita assegna `users.verification_tier = bronze` (enum con tutti e 4 i colori — `none`/`bronze`/`silver`/`gold`/`blue`) e copia il dominio su `users.verified_domain` (colonna denormalizzata, azzerata alla rimozione — evita eager-load della relazione `custom_domain` nei tanti punti che risolvono il nome pubblico di un utente). Lo username di piattaforma resta **sempre** l'identificativo di riserva, citabile/risolvibile — il dominio è un'aggiunta, non una sostituzione a livello di routing/permalink: **diverso** dal "dominio custom associato al blog" ancora in backlog (vedi [§5](#5-sviluppi-futuri-non-ancora-iniziati)), che dipende dal routing per sottodominio non ancora costruito. `GET /api/v1/users/{username}` (e gli endpoint pubblici correlati: posts/blogs/comments/follow) risolvono anche un dominio custom verificato al posto dello username (`_find_user_by_username_or_domain` in `app/api/v1/users.py`) — se non trova corrispondenza esatta sullo username, ritenta sul dominio prima del 404. Dashboard: componente `VerificationBadge` (icona sigillo colorata per tier) + form dominio con istruzioni TXT copiabili, in `dashboard/profile`. Profilo pubblico (`/u/{username}`): stesso `VerificationBadge` accanto al nome; se il dominio è verificato viene mostrato da solo come `@dominio` al posto di `@username` (risolvibile allo stesso modo, vedi sopra), altrimenti resta `@username`. Intestazione che rispetta la preferenza `post_author_name_style` ("Firma i miei post come") invece di una priorità fissa alias→nome/cognome→username. `post_author_name_style` ha un quarto valore, `verified_domain`: firma post/commenti/profilo con il dominio verificato invece che username/nome e cognome/alias (con lo stesso fallback allo username se il dominio non è più verificato). Dashboard: quarta tile in "Firma i miei post come", disattivata (non selezionabile) finché non c'è un dominio verificato attivo. Due utenti che rivendicano lo stesso dominio non ancora reclamato in parallelo ricevono entrambi un `409` pulito invece che un `500` non gestito sul vincolo unique per chi arriva secondo. **Aggiunto in questo blocco**: `silver`/`gold`/`blue` non sono più solo riservati — `app/domain/verification.py::compute_verification_tier` li assegna da tre elenchi gestiti a mano da un Super Admin in `PATCH /admin/config` (`/admin/impostazioni`, sezione "Verifica"): `verification_gold_identifiers` (email/username/domini, entità verificate a mano — testate, agenzie, organizzazioni, personalità note), `verification_silver_identifiers` (email/username, sostenitori economici del progetto) e `verification_blue_domains` (domini email fidati, in aggiunta a quello della piattaforma stessa `NOCT_INSTANCE_FQDN`, già incluso di default). Priorità `gold` > `silver` > `blue` > `bronze` > `none`, mai persa "per errore": ricalcolato ad ogni evento che può farla cambiare (registrazione, cambio email/username, verifica/rimozione dominio custom, modifica di uno dei tre elenchi — che ricalcola tutti gli utenti attivi in un colpo solo, stesso principio della cascata già esistente per `interests` rimossi). **Corretto in questo blocco**: `gold`/`silver` invertiti rispetto alla prima versione — richiesta esplicita, migrazione `b4c5d6e7f8a9` (scambio puro dei nomi delle due colonne, dato preservato). |
| ID fediverse placeholder (AT Protocol + ActivityPub) | ✅ | `atproto_did`/`activitypub_actor_id` in `GET /api/v1/users/{username}` (pubblico) e `GET /api/v1/users/me`: **calcolati al volo**, non persistiti (`app/domain/fediverse.py`), da `NOCT_INSTANCE_FQDN` + l'id UUID dell'utente — mai dallo username, per restare stabili anche se questo cambia (verificato: un cambio username non altera gli ID). `did:web:{dominio}:users:{uuid}` e `https://{dominio}/ap/actors/{uuid}`: nessuna registrazione esterna richiesta (a differenza di `did:plc`), nessun endpoint `/ap/...`/WebFinger reale servito oggi — solo creati e mostrati nella schermata di profilo, come richiesto esplicitamente ("non ancora utilizzati effettivamente"). Federazione vera e propria resta il lavoro di [§5](#5-sviluppi-futuri-non-ancora-iniziati) (AT Protocol prima, poi ActivityPub). |
| Sottotitolo (max 64) e descrizione breve (max 256) del blog | ✅ | `Blog.subtitle` / `Blog.description`, validati in `backend/app/domain/blog_rules.py`; modificabili da `PATCH /api/v1/blogs/{slug}` e in creazione. |
| Visibilità del blog: pubblica / solo iscritti / privata (diario) | ✅ | `Blog.visibility` (`BlogVisibility`). `members` limita le pagine pubbliche del blog a chi è autenticato; `private` le limita al proprietario e ai collaboratori, e la scrittura al **solo proprietario** (`backend/app/domain/authorization.py`). Il feed della homepage mostra solo blog `public`. In dashboard l'elenco blog mostra la visibilità con una banda colorata sul lato destro della card (verde / arancione / bande diagonali rosso-nere). |
| Inviti a collaborare (co-autore / mediatore) con accettazione | ✅ | Tabella `blog_invitations`. Il proprietario crea un invito (`POST /api/v1/blogs/{slug}/invitations`), l'invitato lo accetta/rifiuta dalla propria dashboard (`/api/v1/blogs/received-invitations/...`); solo all'accettazione nasce la `BlogMembership`. Gestione collaboratori (ruolo, rimozione) e revoca inviti lato proprietario. |
| 4 ruoli piattaforma: Super Admin, Amministratore, Moderatore, Utente (default Utente) | ✅ | Enum `PlatformRole` completo (`backend/app/models/user.py`). Super Admin/Amministratore gestiscono utenti e pagine statiche. **Moderatore** ha ora una capacità reale collegata: il pannello di moderazione commenti trasversale (vedi riga sotto e §6) — `require_platform_moderator`/`PLATFORM_MODERATION_ROLES` (`app/api/deps.py`), distinto da `require_platform_admin` che resta solo per Super Admin/Amministratore sulle altre sezioni. Nessun endpoint per promuovere a Moderatore da UI: come per Amministratore/Super Admin, resta un `UPDATE` manuale sul DB. |
| Username e password di Super Admin specificati alla creazione delle risorse (Podman/K8s) | ✅ | `NOCT_SUPER_ADMIN_USERNAME`/`_EMAIL`/`_PASSWORD` (`k8s/secret.example.yaml`, `.env.example`): se tutte e tre valorizzate, l'account viene creato all'avvio del backend se non esiste già (`app/domain/auth.py::bootstrap_super_admin`, chiamata dalla `lifespan` di `app/main.py`) — idempotente, non tocca nulla se l'username/email risultano già in uso. Resta comunque disponibile, come prima, l'auto-promozione del primo utente in modalità `solo` e l'`UPDATE` manuale a DB in modalità `platform`. |
| 4 ruoli blog: Autore (default), Co-Autore, Revisore, Mediatore | ✅ | Enum `BlogRole` (`backend/app/models/blog.py`), tutti e quattro collegati a capacità reali (`backend/app/domain/authorization.py`): scrittura, revisione (`pending_review`→`published`), moderazione commenti. Co-Autore e Mediatore sono assegnabili dal proprietario tramite il flusso di invito (vedi riga "Inviti a collaborare"); Autore/Revisore restano assegnabili solo via DB. |
| Dashboard utente user-friendly, moderna, responsiva | 🟡 | Redesign allineato ai mockup del prototipo (`todo/UX_REDESIGN.md`): shell con sidebar/tab bar, saluto e KPI, post recenti aggregati, tab Panoramica per blog (`GET /api/v1/blogs/{slug}/overview`), stati vuoti/skeleton, interfaccia in italiano e inglese. Letture dei post aggregate per giorno (`post_reads_daily`, `POST /api/v1/posts/{id}/read`: nessun cookie né identificativo del lettore) mostrate nella tab Panoramica con lo spazio occupato su storage. Nessun audit di accessibilità/responsività fatto oltre al testing manuale. **Corretto in questo blocco**: la sidebar di `/dashboard` e `/admin` (`DashboardShell.tsx`) scorreva via col contenuto quando una pagina era più alta del viewport (contenitore `min-h-screen` invece di ancorato all'altezza dello schermo) — ora `lg:h-screen` sul contenitore e scroll isolato al solo `<main>` (la sidebar scrolla autonomamente solo se il menu stesso eccede il viewport). **Aggiunto in questo blocco**: link mancanti per tornare alla piattaforma generale (homepage pubblica, sotto il logo di `DashboardShell`, sia in `/dashboard` sia in `/admin`) e per raggiungere il blog pubblico su cui si sta lavorando (mostrato solo dalle pagine annidate sotto `/dashboard/blogs/{slug}/...`, dedotto dal pathname in `dashboard/layout.tsx`) — prima l'unico modo per identificare/raggiungere il blog attivo era lo slug nella breadcrumb dell'editor di un post, non collegato al sito pubblico né presente altrove (es. impostazioni del blog). Nuove prop `platformLink`/`blogLink` su `DashboardShell` (`components/shell/DashboardShell.tsx`). |
| Dashboard di amministrazione con tutte le funzionalità di moderazione | 🟡 | Sezioni sotto un prefisso URL dedicato `/admin/*` (`frontend/src/app/admin/{pagine,utenti,blog,moderazione,moderazione-commenti,registro}`, `admin/layout.tsx`), separato da `/dashboard/*` che resta solo per le sezioni personali (Profilo, I miei blog, Frammenti, Token API) — non un'app/servizio a parte: un'app separata (provata in un blocco precedente) costringeva a duplicare l'editor delle pagine statiche invece di riusare `RichTextEditor`/`TranslationsBar`, e in modalità `solo` aggiungeva un secondo login senza reale beneficio. `/dashboard` mostra un'unica voce "Amministrazione" (verso `/admin`) per chi ha un ruolo di piattaforma; `/admin` stesso ha un piccolo indice con le sezioni disponibili. Pagine statiche (editor Tiptap completo, come i post), Utenti (ruoli/attivazione — nascosta in modalità `solo`, un solo utente già Super Admin, letto da `GET /api/v1/config`), Tutti i blog (elenco piattaforma con proprietario/visibilità, sospensione — `Blog.is_suspended`, blocca lettura e scrittura pubbliche indipendentemente da `visibility`, vedi `app/domain/authorization.py`), Moderazione post (elenco piattaforma di tutti i post con blog/autore/stato, nascondi/mostra — `Post.is_hidden`, blocca la sola lettura pubblica del singolo post indipendentemente da `status`, l'autore lo vede ancora per poterlo correggere; prima versione, senza motivazione/log/notifica — vedi nota sotto). Moderazione commenti (pannello trasversale, `GET /api/v1/admin/comments` — commenti di *tutti* i blog nello stato indicato, approvazione/rifiuto tramite gli stessi `POST /api/v1/comments/{id}/approve`/`reject` della moderazione per-blog; visibile anche al ruolo Moderatore, non solo ad Amministratore/Super Admin — `frontend/src/app/admin/moderazione-commenti`). |
| Editor pulito, focalizzato sui contenuti (rif. fika.bar) | ✅ | Editor WYSIWYG (Tiptap) con salvataggio in Markdown: titoli (H1-H4), grassetto/corsivo/barrato/codice/link, citazioni, elenchi, tabelle, immagini (con testo ALT), traduzioni (selettore di lingua dalle lingue di fallback del profilo, invece di scrivere la sigla), immagine di copertina, autocomplete delle `@menzioni`, note a piè di pagina. Toolbar fissa in cima allo schermo durante lo scroll (`RichTextEditor`, prop `stickyToolbar`, disattivata nei form annidati come "Aggiungi traduzione"), pulsanti/icone compatti su due righe: la prima sempre visibile (undo/redo, H1-H4, grassetto/corsivo/sottolineato/barrato, allineamento, citazione/codice/link/immagine), la seconda (elenchi, tabella, nota a piè di pagina) mostrata/nascosta da un selettore a freccia all'estrema destra della prima riga (`showRow2`, richiesta esplicita — lascia spazio per aggiunte future senza dover scegliere subito quali pulsanti "sacrificare"; prima erano tutti su una sola riga con scroll orizzontale se lo spazio non bastava), categoria e stato di pubblicazione integrati nella toolbar come dropdown con etichetta. Slug del post proposto automaticamente dal titolo in "Nuovo post" (`lib/slug.ts::slugify`) finché non viene modificato a mano (un solo carattere digitato nel campo slug lo sgancia dal titolo); resta sempre modificabile liberamente. Rail destro (`EditorRail`) sempre visibile fin da "Nuovo post", non solo dopo il primo salvataggio: include già lì stato (bozza/revisione/pubblica, `NewPostStatusControl` — applicato dopo la creazione della bozza incatenando submit-for-review/publish, dato che `POST .../posts` crea sempre una bozza), commenti e crawler del post, come selettori a segmenti (`PostCommentsModeControl`/`PostCrawlingControl`, `components/editor/PostMetaControls.tsx`) invece di `<select>`, condivisi con l'editor del post esistente. Il bottone in alto è sempre "Salva" a prescindere dallo stato scelto nel selettore. Immagini incorporate: stile Bluesky, con pillola "+ ALT"/"ALT" e "×" per rimuoverle in sovraimpressione (niente più `window.prompt` bloccante — `SensitiveImageNodeView`), sfocate con click-per-rivelare anche nell'anteprima dell'editor stesso (non solo sulla pagina pubblica) se segnalate sensibili. Incollare un URL da solo inserisce anche una card di anteprima (titolo/descrizione/immagine Open Graph, `GET /api/v1/link-preview`, pubblico) come blocco a sé sotto il link di testo — quest'ultimo resta libero, cancellabile senza intaccare la card (`LinkPreviewCard`, todo/EDITOR.md). L'endpoint valida schema/IP risolto contro intervalli privati o riservati prima di ogni fetch (`app/domain/link_preview.py::validate_previewable_url`) per limitare l'SSRF, con un rate limiting per IP (30 richieste/minuto). **Cache a due livelli** (richiesta esplicita): Redis come cache calda (TTL 6h) e una tabella dedicata e deduplicata per URL, `link_preview_cache` — condivisa da chiunque citi lo stesso link, un solo fetch reale indipendentemente da quanti post/utenti/pagine lo mostrino (`app/domain/link_preview.py::get_cached_or_fetch_link_preview`). Un'anteprima con dati Open Graph reali resta valida una settimana prima di essere riverificata dal vivo, un fallimento (host irraggiungibile, nessun meta tag, ...) solo 6 ore. Nessun dato salvato nel *contenuto* del post (solo l'URL, come prima): la cache è un livello trasversale, indipendente da dove il link viene citato. Il frontend è passato da `cache: "no-store"` a una finestra a tempo (`frontend/src/lib/markdown.ts::resolveLinkCards`, stessa `REVALIDATE_SECONDS` delle altre fetch pubbliche) ora che il backend stesso mette in cache: la pagina del post con almeno una card di link può tornare cacheabile invece di restare sempre dinamica (chiude il residuo in [§6 riga "Pipeline renderMarkdown"](#6-ottimizzazioni-ed-erogazione)). Riferimenti `[[...]]` e menu `/` sono nel backlog. **Aggiunto in questo blocco**: allineamento del testo (sinistra/centro/destra, su paragrafi e titoli) e sottolineato, mancanti nella toolbar nonostante l'estensione Underline fosse già nello schema TipTap via `StarterKit` (mai esposta da un pulsante — un testo sottolineato con la scorciatoia da tastiera spariva silenziosamente al salvataggio, perché `tiptap-markdown` non aveva una serializzazione per quel mark). Sintassi Markdown dedicata invece di abilitare `html: true` in `markdown-it` (il contenuto salvato può arrivare anche da chiamate dirette all'API, non fidate — vedi il commento su `renderer` in `frontend/src/lib/markdown.ts`): `++testo++` per il sottolineato, `{: .left|.center|.right}` a fine paragrafo/titolo per l'allineamento, entrambe implementate come plugin `markdown-it` condivisi tra editor e renderer pubblico (`frontend/src/lib/markdown-format-extensions.ts`, usati da `components/editor/markdownFormatting.ts` e da `lib/markdown.ts`). Verificato dal vivo: post pubblicato con `++sottolineato++` e `{: .center}`/`{: .right}` produce `<u>sottolineato</u>` e `style="text-align: ..."` nell'HTML pubblico. |
| Blocco di codice con evidenziazione sintassi, copia negli appunti, numerazione righe opzionale | 🟡 | Richiesta esplicita. Distinto dal codice inline (mark `code`, già esistente): pulsante "Blocco di codice" (`{ }`), con un campo di testo libero per il linguaggio (a discrezione di chi scrive: `sql`, `bash`, `php`, `python`, ...) e un pulsante per la numerazione delle righe, entrambi mostrati solo col cursore dentro un blocco di codice. **Spostato in questo blocco** dalla prima riga della toolbar (sempre visibile) alla seconda (collassabile, `showRow2`), non più tra i pulsanti in evidenza: alcuni problemi ancora da analizzare, richiesta esplicita di tenerlo "nascosto" per il momento senza rimuoverlo del tutto — nessun dettaglio sui problemi riportato in questa sessione, da chiarire prima di riportarlo in prima riga. `CodeBlockNode` (`components/editor/markdownFormatting.ts`) estende il `CodeBlock` ufficiale di TipTap invece di quello che `tiptap-markdown` inietta di default (stesso principio di `HeadingNode`/`ParagraphNode`/`UnderlineMark`): il linguaggio è lo standard ` ```lang `, la numerazione righe un'estensione non-CommonMark (seconda parola dell'info string, ` ```python line-numbers `) riconosciuta da un rule renderer `markdown-it` condiviso (`codeBlockMarkdownPlugin`, `lib/markdown-format-extensions.ts`) tra editor e renderer pubblico. **Evidenziazione sintassi** lato renderer pubblico (`lib/markdown.ts`) via **Shiki** (nuova dipendenza, `lib/shiki-highlighter.ts`): un solo highlighter condiviso per il processo Next.js, linguaggi caricati su richiesta (mai tutti i ~200 bundlati) leggendo i fence del documento **prima** del render sincrono di `markdown-it` (l'hook `highlight` di markdown-it è sincrono, il caricamento delle grammatiche Shiki no — l'highlighter condiviso cresce soltanto, mai una race tra richieste concorrenti). Doppio tema chiaro/scuro (`github-light`/`github-dark`, variabili CSS `--shiki-light`/`--shiki-dark`) che segue lo stesso attributo `data-theme` del resto del tema di piattaforma (`globals.css`). Un linguaggio non riconosciuto da Shiki non blocca il rendering: il blocco resta testo semplice senza colore. **Tasto "copia"** su ogni blocco di codice (non solo quelli evidenziati): markup iniettato server-side in `lib/markdown.ts::injectCopyButtons`, click-to-clipboard gestito da un nuovo `CodeCopyProvider.tsx` con delega globale sulla root (`app/layout.tsx`, stesso principio di `LightboxProvider` per lo stesso motivo — il contenuto arriva via `dangerouslySetInnerHTML`). **Numerazione righe**: puro CSS (contatore su `.line`, classe che Shiki già applica a ogni riga), attivata da `data-line-numbers` sul `<pre>`. Verificato dal vivo: post pubblicato con un blocco ` ```python line-numbers ` mostra evidenziazione sintassi corretta (span colorati per token), numerazione righe e tasto copia con il testo esatto del blocco. |
| Note a piè di pagina + bibliografia automatica del blog | ✅ | Elenco strutturato per post (`post_notes`, riscritto ad ogni salvataggio come i tag), non nel corpo. Nel `content` il riferimento è il marcatore link `[n](#nota-n)` inserito dal pulsante «Nota» dell'editor, che apre un **modal** (stesso stile dell'avviso sui contenuti sensibili delle immagini — overlay centrato, non un popover ancorato) con il testo libero come unico campo obbligatorio e, dietro un toggle "Aggiungi dettagli bibliografici", nove campi facoltativi per bibliografie strutturate compatibili BibTeX: titolo, autore, tipo (`kind`: libro/articolo/web/nota, selettore a segmenti), editore/rivista/sito (`source`), anno/data (`issued`, stringa libera), ISBN, DOI, URL, pagina (`post_notes.title/author/kind/source/issued/isbn/doi/url/page`, tutti nullable — `kind`/`source`/`issued`/`url` aggiunti in un blocco successivo a `title`/`author`/`isbn`/`doi`/`page`). Un pulsante "Modifica" per riga nell'elenco sotto l'editor riapre lo stesso modal precompilato per cambiare i campi strutturati dopo l'inserimento. La pagina pubblica del post rende l'elenco numerato in fondo + il testo come tooltip, con una riga di citazione sotto (autore · *titolo* · editore/rivista/sito · anno/data · pagina · ISBN · link doi.org · link generico se diverso dal DOI) quando presente (`frontend/src/lib/markdown.ts::buildNoteCitationElement`); `GET /api/v1/blogs/{slug}/bibliography` e la pagina `/{blog}/bibliografia` raccolgono tutte le note dei post pubblicati, deduplicate per testo, con i post che le citano e la stessa riga di citazione. Stesso schema per i **media** (immagini, `post_media`) e i **link** (`post_links`) citati nel corpo: estratti dal Markdown ad ogni salvataggio (non un elenco a parte come le note), `GET /api/v1/blogs/{slug}/media-bibliography` e `.../links-bibliography`, pagine pubbliche `/{blog}/media` e `/{blog}/link` — raggruppati per URL, con l'elenco dei post che li citano e la data di pubblicazione di ciascuno. **B8**: le note sono anche entità del blog (`blog_notes`: stesse colonne bibliografiche di `post_notes`, `kind` qui NOT NULL con default "note", deduplica per testo normalizzato) con libreria in dashboard (citata in, unione dei duplicati, import/export BibTeX) e filtro per tipo nella bibliografia pubblica — valorizzate solo alla creazione di una nuova nota di libreria a partire da una nota di post (mai sovrascritte su una nota già esistente, per non perdere una modifica manuale fatta da lì; `kind`/`url` scelti direttamente dall'autore hanno la precedenza sulle euristiche `guess_kind()`/`extract_url()`, usate solo come fallback). **Import/export BibTeX ora strutturato** (`app/domain/blog_notes.py::to_bibtex`/`parse_bibtex`): l'export emette `title`/`author`/`publisher`-o-`journal`-a-seconda-del-tipo/`year`/`isbn`/`doi`/`url` come campi separati oltre al testo completo in `note` (fallback per le note create prima di questo campo, sempre presente); l'import li rilegge nei campi strutturati invece di appiattirli tutti nel solo testo. `GET/POST/PATCH .../notes` (libreria) espone ora anche gli stessi campi opzionali di `post_notes` (prima solo `kind`/`url`); **non ancora fatto in questo blocco**: editarli dalla UI della libreria stessa (`NotesTab.tsx`, oggi mostra/edita solo tipo e URL, gli altri campi sono scrivibili solo dall'API), né scegliere in editor una nota già esistente in libreria invece di scriverne sempre una nuova (residuo preesistente). **Corretto in questo blocco**: le tre bibliografie (note/media/link) usano ora la stessa clausola di visibilità pubblica del resto del backend, includendo anche i post pubblicati con `published_at` nullo; il raggruppamento per URL della bibliografia media unisce `is_sensitive` da tutte le citazioni invece di prendere solo la prima incontrata; URL di note create prima della validazione di schema (`_clean_url`) filtrati anche in lettura, non solo in scrittura. |
| Personalizzazione colori/tipografia/presentazione per blog | ✅ | Tabella `blog_configs`, JSON libero con i vincoli della sezione [2](#2-estetica), più `palette_dark` opzionale (stessi vincoli). Palette (chiara e scura), font titoli/corpo/monospace, dimensione del corpo, misura di lettura e layout del feed sono tutti applicati alle pagine pubbliche del blog (`frontend/src/components/blog/BlogPageShell.tsx`): i 15 font curati (`SERIF_FONTS`/`SANS_SERIF_FONTS`/`MONOSPACE_FONTS`) sono self-hostati al build via `next/font/google` (`frontend/src/lib/blog-fonts.ts`, nessuna richiesta a Google a runtime) ed esposti come variabili `--font-blog-*`; `BlogPageShell` punta `--font-heading`/`--font-body`/`--font-monospace` a quella del blog solo se diversa dal default di piattaforma (Lora/Source Sans 3/JetBrains Mono), così un blog mai personalizzato resta identico allo shell. **Aggiunto in questo blocco**: terzo font a scelta per i blocchi di codice (`typography.monospace_font`, elenco curato `MONOSPACE_FONTS` — JetBrains Mono, Fira Code, IBM Plex Mono, Source Code Pro, Space Mono — default JetBrains Mono, richiesta esplicita), stesso principio di validazione/selettore di heading_font/body_font; conta come terzo font verso il limite di 3 di CLAUDE.md § Estetica insieme a titoli/corpo (`backend/app/domain/blog_config.py::MAX_FONTS`, invariato). `typography.body_size`/`measure` guidano dimensione del corpo (`--blog-body-size`) e larghezza della colonna di lettura del post (680px normale, 560px stretta); `layout` (`standard`/`magazine`/`minimal`) sceglie la disposizione del feed della home del blog (`FeedPostCard`, prop `variant`). **Corretta in questo blocco** una regressione: la pagina pubblica del singolo post (`/[blogSlug]/[postSlug]`) non applicava affatto `BlogPageShell`/la palette (nessun fetch di `GET /config`) nonostante la nota qui sotto la desse per fatta — ora la applica come le altre pagine pubbliche del blog. **Corretto anche un bug del validatore**: `validate_blog_config` contava `body_size`/`measure` (stringhe come i nomi dei font) verso il limite di 3 font, impedendo di fatto di salvare dimensione/misura insieme a due font distinti; e il default di piattaforma dichiarato (`DEFAULT_BLOG_CONFIG`) diceva `body_font: "Inter"` mentre il font sans-serif reale della piattaforma è Source Sans 3 — un blog mai personalizzato risultava quindi "diverso dal default" appena la tipografia è stata applicata al rendering. |
| Immagine di copertina e favicon dedicate per blog | ✅ | Entrambe facoltative, gestibili solo dal proprietario (`SettingsTab`, sezione Identità). **Cover** (banner della home pubblica, `Blog.cover_image_url`): stessa pipeline di moderazione automatica e avviso manuale dei media di contenuto (`POST /api/v1/blogs/{slug}/cover-image`, `PATCH .../cover-image` per le sole categorie, `DELETE .../cover-image`) — sfocata e cliccabile per rivelarla se segnalata, stesso meccanismo della cover dei post. Sostituire/rimuovere la cover non cancella l'oggetto precedente su storage, stessa scelta già fatta per `Post.cover_image_url`. **Favicon** (icona di identità nella scheda del browser, `Blog.favicon_object_key`): nessuna moderazione (non è contenuto), stesso bucket pubblico degli avatar con prefisso `favicons/{blog_id}/`, formati PNG/JPEG/WEBP fino a 512 KiB (`POST`/`DELETE /api/v1/blogs/{slug}/favicon`) — sostituirla cancella l'oggetto precedente, come per l'avatar utente. Applicata a tutte le pagine pubbliche del blog tramite `frontend/src/app/[blogSlug]/layout.tsx` (solo `generateMetadata`, stesso principio di `u/[username]/layout.tsx`): un blog senza favicon propria resta con quella di piattaforma (`src/app/favicon.ico`, convenzione file di Next.js). Oltre alla scheda del browser, la favicon (quando impostata) sostituisce anche il placeholder colorato con l'iniziale del titolo ovunque il blog è rappresentato come icona: intestazione della pagina del blog, directory `/blogs` (griglia e sidebar) e tab "Blog" del profilo pubblico. |
| Sistema di commenti con moderazione dell'autore | ✅ | **Tre stati** (`Blog.comments_mode`): `members` (default, solo utenti registrati), `everyone` (chiunque, richiede il captcha Cloudflare Turnstile per gli anonimi — vedi riga dedicata sotto), `closed` (nessuno). **Override per singolo post** (`Post.comments_mode`, `null` = eredita dal blog) dall'editor. Commenti di autori non registrati sempre moderati prima della pubblicazione, indipendentemente dal captcha. **Thread di risposte**: `Comment.parent_id`, un livello di indentazione visiva nel frontend (si può rispondere solo a un commento di primo livello, non a una risposta — mantiene il thread leggibile senza limitare la profondità a livello di dati). UI pubblica in `frontend/src/components/CommentsSection.tsx`, integrata nella pagina del post — **prima di questo blocco non esisteva affatto un'interfaccia pubblica per leggere/scrivere commenti**, solo l'API e la moderazione lato dashboard. **B4 (todo/UX_REDESIGN.md)**: lista dei bloccati per blog (utente o hash dell'email anonima), segnalazione di un commento ai moderatori di piattaforma con nota obbligatoria (`POST /api/v1/comments/{id}/report`, coda `GET /api/v1/admin/comments?reported=true`), chiusura automatica dei commenti N giorni dopo la pubblicazione (`Blog.comments_auto_close_days`). |
| Captcha sui commenti aperti a tutti | ✅ | Cloudflare Turnstile (`app/core/captcha.py::verify_turnstile`, fail closed: un servizio irraggiungibile blocca il commento invece di lasciarlo passare, a differenza della moderazione immagini). Senza `NOCT_TURNSTILE_SITE_KEY`/`_SECRET_KEY` configurate, `comments_mode="everyone"` non è impostabile su nessun blog/post (`400` esplicito) — fail closed anche sulla *funzionalità*, non solo sulla singola richiesta. Site key esposta pubblicamente da `GET /api/v1/config` (safe, a differenza della secret key) perché il frontend sappia se mostrare il widget. |
| Frammenti: raccolta personale di porzioni di testo evidenziate dal lettore | ✅ | Selezionando del testo con il mouse su un post pubblicato (max 15% del testo, `app/domain/fragments.py`) appare un menu contestuale per salvarlo (`POST /api/v1/posts/{post_id}/fragments`, richiede sessione — a livello di **utente**, non di blog). **Pubblico o privato** (`PostFragment.is_public`, scelto al salvataggio con due pulsanti — "Salva (privato)"/"Salva (pubblico)" — e modificabile anche ex-post dalla raccolta, `PATCH /api/v1/fragments/{id}`): privato = visibile solo a chi lo ha salvato (default, comportamento storico), pubblico = visibile ad altri utenti iscritti alla piattaforma, mai a visitatori anonimi. **Nota**: oggi "pubblico" è solo il dato e la possibilità di consultarlo/impostarlo — non esiste ancora una superficie che mostri ad *altri* utenti i frammenti pubblici altrui (es. evidenziarli nella pagina del post insieme ai propri): passo successivo naturale, non affrontato in questo blocco. Raccolta unificata in `/dashboard/frammenti` (`GET /api/v1/users/me/fragments`): testo, autore, titolo del post, data di cattura, click verso il permalink originale, pulsante di visibilità accanto a "Rimuovi". Il testo salvato (non un offset) viene ri-cercato nel post reso ad ogni lettura successiva e ri-evidenziato con un `<mark>` ciano tenue (`frontend/src/lib/highlight-fragments.ts`, `components/FragmentReader.tsx`), indipendentemente dal fatto che si sia arrivati dalla pagina di raccolta. Ri-selezionare (anche solo in parte) un frammento già evidenziato propone "Rimuovi frammento" invece di "Salva frammento" (`findOverlappingFragmentIds`, via `Range.intersectsNode`) — **limite noto**: una selezione a cavallo di due paragrafi distinti potrebbe non ri-evidenziarsi (si veda il commento in quel file), il salvataggio resta comunque intatto. |
| Multilingua per post e pagine statiche | ✅ | Schema "famiglia di traduzioni" (`translation_group_id` + `locale` + slug per lingua). Il routing `/it/`, `/en/`, ... è lasciato al frontend: **il frontend pubblico multilingua/i18n-routing non è ancora costruito**, oggi si seleziona la lingua via parametro (chip lingua nel feed della home, `?locale=`). **Lingua dell'interfaccia** (etichette/menu, distinta da quella dei contenuti): `next-intl` con `it`/`en` in `frontend/src/messages/`, risolta da cookie → `Accept-Language` → `it`; default di piattaforma da `platform_config.default_locale` (Super Admin, `PATCH /api/v1/admin/config`; seme `NOCT_DEFAULT_LOCALE`) e preferenza per account `users.ui_locale`. |
| Pagine statiche del sito principale (Chi siamo, Contatti, Privacy) | ✅ | CRUD gestito da Amministratore/Super Admin (sempre attive), `GET /api/v1/pages`. Rendering pubblico su `/p/{slug}` (prefisso dedicato per non collidere con gli slug dei blog raggiungibili senza sottodominio). Estese anche ai blog come feature opt-in per il proprietario (`Blog.static_pages_enabled`, disattiva di default): stessa interfaccia di editing (editor WYSIWYG senza il pulsante "Nota", niente tag/categorie/stato di pubblicazione a workflow), stesso schema di traduzione (`translation_group_id`+`locale`+`slug`) di post e pagine di piattaforma — incluso lo stesso selettore di lingua con le pillole delle traduzioni già esistenti (`GET /{page_id}/translations` e `GET /{slug}/pages/{page_id}/translations`, componente frontend `TranslationsBar` condiviso con l'editor dei post). Permalink pubblico `/{blog_slug}/pagina/{slug}`. `backend/app/api/v1/blogs/pages.py` (endpoint `/{slug}/pages...`), `backend/app/domain/pages.py`. |
| Interessi utente (tag fissi multilingua, max 5, per trovare/seguire persone con gli stessi) | ✅ | Elenco configurabile, non testo libero: chiave canonica non linguistica (es. `music`) + etichetta per lingua (`{"key": "music", "translations": {"it": "Musica", "en": "Music"}}`), persistito in `platform_config.interests` (JSONB) — seminato da `NOCT_DEFAULT_INTERESTS` (JSON) o da un elenco builtin curato (`app/domain/interests.py::DEFAULT_INTERESTS`, 16 voci it/en/de/fr) alla prima installazione, poi modificabile in qualsiasi momento da un Super Admin (`PATCH /admin/config`, sezione "Interessi utente" in `/admin/impostazioni`: chiave non più modificabile dopo la creazione, etichetta per ogni lingua aggiunta — **corretto in questo blocco**: l'interfaccia mostrava solo due colonne fisse IT/EN, l'unico modo per aggiungere una lingua diversa era `NOCT_DEFAULT_INTERESTS` all'avvio anche se il backend non ha mai limitato `translations` a nessun set di lingue; ora `InterestTranslationsEditor` in `app/admin/impostazioni/page.tsx` accetta un codice lingua libero per ogni etichetta, sia sugli interessi esistenti sia in fase di creazione). `GET /api/v1/interests` (pubblico) espone l'elenco corrente; `User.interests` (nuovo `ARRAY(String)`, migrazione `4d23cdb0ccc3`) è la scelta dell'utente, al più 5 chiavi valide, in `PATCH /users/me` — enforcement applicativo (`app/domain/interests.py::validate_user_interest_keys`), non a DB, stesso principio dei tag sui post. Esposti su `GET /users/{username}` (profilo pubblico, chip cliccabili verso `/users?interest=...`) e su `GET /users?interest=...` (filtro della directory, per trovare persone con l'interesse condiviso e seguirle tramite il follow già esistente — nessun meccanismo di follow separato "per interesse", si usa `POST /users/{username}/follow`). Picker in `dashboard/profile` (sezione Identità, sotto la bio). **Corretto in questo blocco**: rimuovere una chiave da `platform_config.interests` (`PATCH /admin/config`) non toccava le selezioni già salvate su `User.interests` — restavano chiavi orfane, senza più nessuna traduzione associata. `PATCH /admin/config` ora ripulisce anche `User.interests` di ogni utente interessato (query con l'operatore array `&&`, `User.interests` è un `ARRAY` "core" senza il comparator `.overlap()` del dialect Postgres). Verificato dal vivo: utente con `interests: ["music"]`, rimossa "music" dalla configurazione, `GET /users/me` torna `interests: []`. |
| Directory pubblica degli utenti, con ricerca | ✅ | `GET /api/v1/users` (pubblico): stesso schema pragmatico di `GET /blogs` (`ILIKE` su username/alias pubblico/bio, filtro `locale` per lingua madre, `sort` `new`/`followers`) — solo account attivi che non hanno scelto l'opt-out (`User.directory_listed`, attiva di default, migrazione `c9839d73bf05`): il profilo resta comunque sempre raggiungibile dal link diretto `@username`, il flag esclude solo dall'elenco/ricerca. Toggle in `dashboard/profile` (sezione Privacy e dati). Frontend: `/users` (voce "Persone" in `SiteHeader`), card con avatar/alias/bio/follower come `/blogs`. **Corretto in questo blocco**: il Super Admin è ora **sempre** escluso dalla directory (`app/api/v1/users.py::list_public_users`, condizione `platform_role != SUPER_ADMIN` in aggiunta a `directory_listed`), non un'opzione modificabile dall'utente — per sicurezza, evitare che l'account con i privilegi più ampi sia individuabile dalla directory pubblica, superficie utile per un attacco a forza bruta sulle credenziali; il toggle in `dashboard/profile` è disattivato (sempre spento) per quell'account, con nota dedicata. |
| Follow tra utenti e verso i blog | ✅ | Due meccanismi separati: `UserFollow` (segue l'utente con il suo username) e `BlogFollow` (segue un blog, che può presentarsi con un alias tramite `Blog.default_author_display_name` senza rivelare chi lo gestisce). `GET /api/v1/feed/posts?following=true` restituisce i post dei blog/utenti seguiti (tab "Seguiti" della home). `GET /api/v1/users/me/follow-stats` somma i due conteggi per il solo proprietario (unico punto in cui username e alias dei suoi blog compaiono insieme); pubblicamente ogni entità continua a mostrare solo il proprio numero di follower, separatamente. A supporto dell'anonimato, `GET /api/v1/blogs/{slug}` non espone più `owner_id` a nessuno tranne il proprietario stesso (`_to_blog_out`) — **residuo noto**: `Post.author_id`/`PostOut.author_id` restano invece l'id reale dell'autore anche sui blog che usano un alias, non ancora mascherati allo stesso modo (nessun uso lato frontend oggi, ma un potenziale canale di correlazione se in futuro venisse esposto). |
| Profilo utente: bio, avatar, link social | ✅ | Avatar su MinIO/S3. Bio estesa con nome, cognome, paese, lingua madre e lingue di fallback (queste ultime anche come lingue verso cui l'utente potrà eventualmente tradurre i propri contenuti — stile fika.bar). Link social: elenco piattaforme con icona monocromatica in un file di configurazione lato frontend (`frontend/src/lib/social-platforms.tsx`), facilmente editabile; il backend resta agnostico (stringa libera). L'avatar, quando impostato, sostituisce il placeholder con l'iniziale del nome ovunque compare un autore: card del feed (`FeedPostCard`/`FeedPostCardClient`), pagina del post, commenti (`CommentOut.author_avatar_url`), elenco collaboratori del blog (`MemberOut.avatar_url`) e autocomplete delle @menzioni nell'editor (`MentionableUserOut.avatar_url`) — tutti calcolati da colonne già in memoria nelle stesse query batched esistenti (nessuna query aggiuntiva, nessun eager-load di relazioni). |
| Nome pubblico predefinito per gli autori/co-autori di un blog | ✅ | `Blog.default_author_display_name`: se valorizzato è **imposto** come nome autore sui post di quel blog (todo/USERS.md #2), a meno che il singolo collaboratore non abbia un proprio alias di membership che ha la precedenza. Nessun override per singolo post. In creazione (dashboard) il campo è pre-compilato con lo username di chi crea il blog — solo un suggerimento visibile ed editabile, non un vincolo: se lasciato vuoto in `POST /api/v1/blogs` il nome pubblico ricade comunque sullo username tramite la stessa risoluzione. |
| Gestione contenuti in Markdown, nessun rendering lato backend | ✅ | Rendering a HTML e sanificazione lato frontend (Server Component + `markdown-it`/DOMPurify) sulla pagina pubblica del post — vedi permalink più sotto. |
| UUID come chiave primaria per ogni entità | ✅ | |
| Media su S3 in `.../userdata/{user_uuid}/{blog_uuid}/media/...` | ✅ | Bucket con policy pubblica scoped al solo prefisso `media/*`; stesso endpoint di upload usato sia per le immagini incorporate nel contenuto sia per la cover del post (`Post.cover_image_url`). **B7**: libreria media per blog (`media_files`, `GET/PATCH/DELETE /api/v1/blogs/{slug}/media`) con alt, didascalia, avviso e "usata in". **Corretto in questo blocco**: `POST .../media/sync` raggruppava per URL prima di inserire ma prendeva `is_sensitive` dalla prima riga incontrata (query senza `ORDER BY`, ordine non deterministico) — ora unisce il flag da tutte le occorrenze duplicate. **Corretto in un blocco successivo** (trovato dal vivo, non dai test): `used_in` era calcolato solo da `post_media` (immagini incorporate nel contenuto), mai da `Post.cover_image_url` — un'immagine usata solo come cover di un post risultava "libera" in libreria e **cancellabile mentre era ancora la cover live del post**. La cover di un blog inoltre non passava proprio dall'endpoint della libreria (`POST .../cover-image` scriveva solo su storage): non ci compariva mai, a differenza della cover di un post (stesso endpoint upload di `POST .../media`). Corretto: `_usages`/il controllo di cancellazione ora considerano anche `Post.cover_image_url`; `POST .../cover-image` del blog crea sempre una riga `MediaFile` (nuovo campo `used_as_blog_cover` in `MediaFileOut`); `DELETE` rifiuta anche se l'immagine è l'attuale cover del blog. Aggiunto anche `cover_image_alt_text` (mancava del tutto) sia su `Post` sia su `Blog`, stesso schema tri-state di `cover_image_categories` — UI in `CoverImageUpload.tsx` (campo alt text sotto l'anteprima, salvato `onBlur` per non spammare l'API a ogni tasto quando il chiamante salva subito, come `SettingsTab.tsx`). Verificato dal vivo: immagine caricata come cover di un post → 409 alla cancellazione finché resta la cover, 204 dopo averla tolta; stesso per la cover del blog, ora presente in libreria con `used_as_blog_cover: true`. **Aggiunto in questo blocco**: pulsante "Immagine" dell'editor e `CoverImageUpload` (solo cover di un *post*, non la cover del blog in `SettingsTab` — lì non esiste un endpoint per impostare la cover da URL, solo da upload) aprono ora `ImagePickerModal.tsx`, un modal a tre schede (stesso stile a overlay di `ContentWarningModal`/`NoteModal`) che affianca al caricamento diretto (comportamento preesistente, invariato) la scelta di un'immagine già presente nella libreria del blog corrente (`GET .../media`) o aggregata su tutti i blog dell'utente (`GET /api/v1/users/me/media`, endpoint già esistente per `/dashboard/media`, qui riusato in selezione oltre che in sola consultazione) — nessun endpoint nuovo lato backend. Nessuna integrazione Unsplash o altri portali esterni in questo blocco (rimandata esplicitamente). |
| Moderazione automatica delle immagini (nudità/contenuti sensibili) | ✅ | Servizio self-hosted separato (`moderation/`, modello Falconsai/nsfw_image_detection) — nessuna immagine lascia l'infrastruttura. Fail open: un problema del servizio non blocca mai l'upload. Immagini segnalate: sfocate e cliccabili per rivelarle, sia nel contenuto sia in copertina. Soglia di confidenza non ancora tarata su un campione reale (0.7 di default, scelta prudente). L'autore può anche impostare manualmente un avviso, con un modal stile Bluesky (Suggestivo/Nudità/Esplicito/Contenuto sensibile — `app/domain/content_media.py::SENSITIVITY_CATEGORIES`): per le immagini nel contenuto viaggia nello stesso `title` Markdown di "sensitive" (`sensitive:categoria1,categoria2`, sincronizzato in sola lettura in `post_media` per la bibliografia dei media), per la cover in `Post.cover_image_categories` (colonna dedicata, indipendente da `cover_image_is_sensitive`: impostarla forza comunque il flag a True). Selezionare zero categorie rimuove del tutto l'avviso, anche se messo dall'automoderazione — scelta finale sempre dell'autore. **Lightbox** (`components/Lightbox.tsx`, montata una sola volta in `app/layout.tsx`): ogni immagine pubblicata (contenuto del post, copertina di blog/post, griglia media) si ingrandisce a schermo intero al click/tap. Per le immagini non segnalate il click apre subito la lightbox (`img[data-lightbox]`); per quelle segnalate come sensibili il primo click resta solo la rivelazione già esistente (trucco CSS label→checkbox) — mai le due cose insieme sullo stesso click — e solo dopo compare, in alto a destra, un pulsante con freccia diagonale (`.lightbox-expand-btn`) per ingrandirle a sua volta. Nessun listener per immagine: un solo click delegato a livello di `document` dentro `LightboxProvider` intercetta entrambi i marcatori, quindi la pagina del post/blog resta un Server Component (l'HTML del contenuto arriva già marcato da `lib/markdown.ts`). **Corretto in questo blocco**: un'immagine senza testo alternativo (nel corpo del post, in copertina o nella griglia della bibliografia media) diventava un controllo da tastiera senza nome accessibile una volta reso cliccabile per la lightbox — aggiunto un `aria-label` di fallback nei tre punti coinvolti. |
| Backup/fallback Markdown dei post su S3 ad ogni salvataggio | ✅ | Privato, via coda RabbitMQ + worker dedicato (`worker-post-backup`). |
| Stato post: draft → pending_review → published, con `published_at` per pianificazione | ✅ | Ruolo Revisore collegato alla transizione `pending_review`→`published`. |
| Hostname/FQDN e modalità di installazione via env | ✅ | `NOCT_INSTANCE_FQDN`, `NOCT_DEPLOYMENT_MODE=solo\|platform`. |
| Opt-in per crawler: motori di ricerca e IA/LLM separatamente, per blog e per post | ✅ | `Blog.search_indexing_enabled`/`ai_crawling_enabled` (bool, default `true`), con override per singolo post (`Post.search_indexing_enabled`/`ai_crawling_enabled`, `null` = eredita dal blog — un post non può "riaprire" un crawler già escluso a livello di blog, solo restringerlo ulteriormente, vedi `app/domain/seo.py`). Applicato via `robots.txt` generato dinamicamente (`frontend/src/app/robots.ts`, `GET /api/v1/seo/crawl-directives`): `Disallow` mirato per un elenco curato di crawler IA/LLM noti (GPTBot, ClaudeBot, CCBot, Google-Extended, ...) distinto dal gruppo `User-agent: *` dei motori di ricerca tradizionali — una richiesta di rispetto dello standard, non un blocco tecnico garantito (un crawler che ignora robots.txt non è fermato). Sulla pagina pubblica del post, `search_indexing_enabled=false` aggiunge anche il meta tag `<meta name="robots" content="noindex,nofollow">` (`generateMetadata` in `[blogSlug]/[postSlug]/page.tsx`). Impostabile dal proprietario in `dashboard/blogs/{slug}` (tab Impostazioni) e per singolo post dalla toolbar dell'editor. |
| Ottimizzazione SEO | ✅ | `metadataBase` (nuova env di build `NEXT_PUBLIC_SITE_URL`, `frontend/src/lib/site.ts`) + template titolo (`%s · Notturni`) in `layout.tsx`. Canonical (`alternates.canonical`) e Open Graph/Twitter card su tutte le pagine pubbliche: homepage, homepage di blog, post (tipo `article`, immagine di copertina se non sensibile), pagine statiche di piattaforma **e di blog**, bibliografia/media/link del blog, **profilo pubblico utente** (`/u/{username}`). `sitemap.xml` (`frontend/src/app/sitemap.ts`, `GET /api/v1/seo/sitemap-entries`) e `robots.txt` (vedi riga sopra) generati dinamicamente, entrambi coerenti con `search_indexing_enabled` effettivo (un post/blog escluso non compare in nessuno dei due). **Dati strutturati JSON-LD** (`BlogPosting`, `frontend/src/components/blog/JsonLd.tsx`) sulla pagina del post: titolo, estratto, URL, data di pubblicazione, lingua, autore, blog di appartenenza, immagine di copertina se non sensibile — `dateModified` volutamente assente (nessun campo "ultima modifica" sul modello `Post`, solo `created_at`/`published_at`). **`/u/{username}` era un Client Component** (dati interattivi — follow, tab post/blog/commenti — richiedono la sessione dell'utente), invisibile ai crawler che non eseguono JS (nessun dato nell'HTML iniziale): convertito a Server Component (blocco performance/SEO successivo). Profilo, post/blog/commenti pubblici e follower sono ora risolti server-side (`getPublicUserProfile`/`getPublicUserPosts`/`getPublicUserBlogs`/`getPublicUserComments`/`getPublicUserFollowers` in `server-api.ts`, tutte pubbliche); resta un solo client component isolato, `FollowUserButton` (`components/profile/FollowUserButton.tsx`), per il follow/unfollow che richiede la sessione del visitatore — riceve la lista follower già risolta dal server, nessuna fetch propria. Lo switch tra le tab Post/Blog/Commenti (`ProfileTabs`) mostra/nasconde con `hidden` contenuto già renderizzato, non rifà fetch. `layout.tsx` continua a esporre `generateMetadata` (dedup automatico del fetch di `getPublicUserProfile` con la pagina). **Non affrontato in questo blocco**: metadata (canonical/OG/JSON-LD) sulle pagine statiche di blog e di piattaforma non toccate qui restano quelle già esistenti; `dateModified`/aggiornamento incrementale della sitemap in caso di modifica senza nuova pubblicazione. |

## 2. Estetica

| Specifica | Stato | Note / task residuo |
|---|---|---|
| Palette di massimo 5 colori | ✅ | Validato server-side in `backend/app/domain/blog_config.py`. |
| Massimo 3 font | ✅ | Idem. |
| Titoli in serif, testo/link in sans-serif | ✅ | Applicato nel tema di default del frontend **e** imposto come vincolo sulla personalizzazione: `backend/app/domain/blog_config.py` valida `heading_font`/`body_font` contro due elenchi curati di Google Fonts (`SERIF_FONTS`/`SANS_SERIF_FONTS`), `AppearanceTab.tsx` mostra solo quei font in un menu a tendina invece del campo libero. Altre chiavi di `typography` restano libere. **Nota**: la personalizzazione di palette/tipografia non è ancora applicata al rendering pubblico del blog (solo salvata/validata) — gap preesistente, non affrontato qui. |
| Toni calmi, non aggressivi | ✅ | Guardrail automatico in `validate_blog_config`: ogni colore della palette è convertito in HLS, saturazione oltre il 90% (`MAX_SATURATION`) viene rifiutata con `400` — oltre al solo limite sul numero massimo di colori già presente. |

## 3. Architettura, stack e infrastruttura

| Specifica | Stato | Note / task residuo |
|---|---|---|
| Stack di base: Next.js, FastAPI, PostgreSQL 16+, Redis, RabbitMQ, MinIO | ✅ | Tutti i servizi presenti in `compose.yaml` e nei manifest `k8s/`. **Corretto in questo blocco**: mancavano da `k8s/` i Deployment dei consumer di coda `worker-email-otp`/`worker-post-backup`/`worker-newsletter` (senza, MFA/reset password via email, backup dei post e invio newsletter non avrebbero funzionato in produzione — solo l'accodamento su RabbitMQ, mai il consumo) e del servizio di moderazione immagini `moderation` (fail open per design, ma nessuna immagine sarebbe stata moderata) — mancava anche `NOCT_MODERATION_SERVICE_URL` in `configmap.yaml`. Nuovi manifest `k8s/worker-*.yaml`/`k8s/moderation.yaml`, registrati in `kustomization.yaml`. |
| `backend/schema.sql` allineato e `alembic upgrade head` funzionante | ✅ | **Corretto in questo blocco**: Alembic aveva due head divergenti (`4d23cdb0ccc3`, interessi utente; `769f5009bad5`, newsletter), creati su branch paralleli senza una migrazione di merge dedicata — `alembic upgrade head` falliva con "Multiple head revisions", il che avrebbe bloccato qualunque job di migrazione in un deploy reale, non solo la rigenerazione di `schema.sql`. Nuova migrazione di merge (`a0579cc46db6`, stesso principio già usato da `cf49d59f6471` per una situazione identica), `schema.sql` rigenerato col comando documentato in testa al file (`alembic upgrade head --sql`). Verificato `upgrade head`/`downgrade`/`upgrade head` puliti su un DB reale. |
| Redis per rate limiting e lock distribuiti | 🟡 | **Rate limiting**: `app/domain/rate_limit.py::enforce_rate_limit` (contatore a finestra fissa, `INCR`+`EXPIRE`), fail open se Redis non è raggiungibile (stesso principio del servizio di moderazione immagini). Applicato a `POST /api/v1/auth/login` (20 tentativi/5 min per IP, 5/5 min per email), `/mfa/verify` + `/mfa/totp/confirm` + `/mfa/email/confirm` (20/5 min per IP, 8/5 min per soggetto del codice — un codice a 6 cifre è altrimenti indovinabile), `/password/forgot` + `/password/reset` (20/5 min per IP, 5/5 min per email) e `GET /api/v1/link-preview` (30 richieste/minuto per IP — mitiga l'uso come proxy/scanner, endpoint pubblico senza autenticazione). **X-Forwarded-For**: `app/core/http.py::client_ip` si fida dell'**ultimo** hop (scritto da Traefik, unico proxy noto davanti al backend), non del primo — un client non può più spoofare l'IP usato da rate limiting/audit log mettendone uno a piacere in testa all'header. **Manca ancora**: lock distribuiti (nessun caso d'uso che li richieda oggi). |
| Dockerfile/manifest agnostici rispetto all'architettura (x86_64/ARM64) | ✅ | Immagini multi-arch, nessuna dipendenza hard-coded da un'architettura. |
| Compatibilità S3 con endpoint custom iniettato (MinIO/AWS/Cloudflare) | ✅ | `backend/app/core/storage.py`, sempre via `NOCT_S3_ENDPOINT_URL` configurabile (opzionale per AWS S3 vero). Auth via `NOCT_S3_ACCESS_KEY_ID`/`_SECRET_ACCESS_KEY` se entrambe valorizzate, altrimenti ruolo AWS (default credential chain di boto3); `NOCT_S3_REGION` opzionale. |
| Backend di storage alternativo su filesystem locale (`NOCT_STORAGE_BACKEND=localstorage`) | ✅ | Pensato per installazioni `solo` self-hosted senza voler gestire uno storage S3-compatible: file scritti sotto `NOCT_LOCAL_STORAGE_BASE_PATH` e serviti dal backend stesso (`StaticFiles` su `/storage`, `app/main.py`). Non cablato in `compose.yaml`/`k8s/` come scelta attiva (resta MinIO/S3), solo documentato in `.env.example`. Nessun volume persistente dedicato nei manifest K8s per questa modalità. |
| Prefisso env `NOCT_`/`noct_` | ✅ | |
| Clusterizzazione dei componenti in produzione | ⚪ | Manifest K8s attuali sono un primo draft a singolo nodo; nessun lavoro di clustering/HA fatto. |
| Traefik come ingress | ✅ | `k8s/ingress.yaml` (`ingressClassName: traefik`, catch-all path-based, usato anche per il primo test senza dominio reale) **+** `k8s/ingressroute.yaml` (`IngressRoute` Traefik, `HostRegexp` per il routing dinamico per sottodominio-per-blog, `slug.notturni.eu` — le due risorse convivono). Verificato dal vivo su Podman compose con `Host` forzato via curl: home, post, feed.xml e sottodomini riservati (`www.`) tutti risolvono correttamente. Regex Traefik (RE2) senza lookahead: l'esclusione delle etichette riservate (`www`, `blog`, `mail`, ...) è applicata lato Next.js (`frontend/src/proxy.ts::RESERVED_SUBDOMAINS`, stessa lista di `blog_rules.py`), non nel match Traefik — commento dedicato in `ingressroute.yaml`. Security header minimi (HSTS/`X-Content-Type-Options: nosniff`/`X-Frame-Options`/`Referrer-Policy`) via `k8s/middleware-security-headers.yaml` (Traefik `Middleware`), applicato a entrambe le risorse. TLS wildcard per `*.notturni.eu` in produzione: vedi riga dedicata "cert-manager / Let's Encrypt in produzione" più sotto — nota qui obsoleta, corretta in questo blocco (era ancora segnata "manca" nonostante fosse già stata implementata in un blocco precedente). **Corretto in questo blocco, bug scoperto sull'istanza pubblica**: la Content-Security-Policy non vive più in `middleware-security-headers.yaml` — era statica (stesso `script-src 'self'` per ogni richiesta, senza nonce/`unsafe-inline`), e senza nonce quella direttiva blocca anche gli script inline che Next.js stesso inietta per l'idratazione (payload RSC in streaming), non solo script di terze parti: la pagina veniva servita (200) ma nessun form/bottone rispondeva ai click — login e registrazione inclusi, su un'istanza altrimenti sana (API testata via `curl` diretto, 200/201 corretti). La CSP è ora generata per-richiesta con un nonce fresco in `frontend/src/proxy.ts` (nuovo `NonceProvider`/`useNonce` per propagarlo ai client component che iniettano un proprio `<Script>`, es. il widget Turnstile in `CommentsSection.tsx`); `connect-src` è derivato dagli URL reali configurati (`NEXT_PUBLIC_SITE_URL`/`NEXT_PUBLIC_API_URL`) invece di un dominio hardcoded, altrimenti la stessa CSP pensata per la produzione avrebbe bloccato anche il dev locale (frontend/API su origini/porte diverse). Nessun impatto sul rendering: l'app è già interamente dinamica (next-intl legge un cookie per la lingua in ogni pagina), il requisito di Next.js "tutte le pagine devono essere a rendering dinamico per usare un nonce" era già soddisfatto. Verificato dal vivo con un browser headless (Playwright) sia sull'istanza pubblica prima del fix (errore CSP in console, form non interattivi) sia in locale dopo il fix (registrazione end-to-end completata, redirect a `/dashboard`, nessun errore CSP/idratazione). |
| Longhorn come storage class | ✅ | Usato in `k8s/postgres.yaml` e `k8s/minio.yaml` (draft). |
| Backup su S3 esterno di database e MinIO | ✅ | `app/workers/backup.py`, job idempotente da eseguire a intervalli (stesso stile di `audit_maintenance.py`, `--loop`/`--interval`): `pg_dump` in formato custom di Postgres + mirror di tutti i bucket applicativi S3/MinIO (contenuti, avatar, audit), entrambi scritti su uno storage S3 **esterno** dedicato (`NOCT_BACKUP_S3_BUCKET`/`_ENDPOINT_URL`/`_REGION`/`_ACCESS_KEY_ID`/`_SECRET_ACCESS_KEY` — endpoint/credenziali sempre iniettabili come per lo storage applicativo, ma potenzialmente un provider diverso). `NOCT_BACKUP_S3_BUCKET` assente disattiva l'intero worker. Servizio `worker-backup` in `compose.yaml` (in locale punta allo stesso MinIO, bucket dedicato — solo per avere qualcosa di verificabile in sviluppo), `CronJob` `k8s/backup.yaml` (le 02:00 UTC, un'ora prima di `audit-maintenance`). Richiede `pg_dump` nell'immagine (`postgresql-client`, aggiunto al `Dockerfile`). Non gestisce ancora la cancellazione dei backup vecchi (retention) né il ripristino automatico — solo scrittura, come un primo passo. |
| Servizio SMTP containerizzato (Podman e Kubernetes) | ✅ | `boky/postfix`, sia su `compose.yaml` (servizio `smtp-relay`) sia su `k8s/smtp-relay.yaml` (Deployment+Service) — un'opzione in più oltre a puntare `NOCT_SMTP_HOST` (vedi riga "MFA Email OTP asincrono") direttamente a un provider esterno. Due modalità, scelte con `NOCT_SMTP_RELAY_HOST` (vuoto di default): **invio diretto** (il container spedisce da sé — richiede comunque un dominio con SPF/DKIM/reverse DNS a posto per una buona deliverability, cosa che containerizzarlo da solo non risolve) oppure **inoltro** verso uno smarthost esterno con autenticazione (`NOCT_SMTP_RELAY_USERNAME`/`_PASSWORD`, un provider transazionale reale con deliverability già garantita). Non è il default di `worker-email-otp`, che resta puntato a `mailhog` per non richiedere credenziali reali in sviluppo. Verificato dal vivo in modalità inoltro: un'email inviata al servizio è stata davvero recapitata a destinazione (`mailhog`, usato qui solo come destinazione di prova). |
| Audit log delle azioni sensibili | 🟡 | **Scrittura**: tabella `audit_log` (append-only, senza FK, `payload` JSONB) + helper sincrono `app/domain/audit.py::record()` nella stessa transazione dell'azione. Eventi agganciati: `auth.login` (password/MFA/SSO), `auth.login_failed`, `user.role_change`, `user.activated`/`user.deactivated`, `blog.suspended`/`blog.unsuspended`, `post.hidden`/`post.unhidden`, `comment.approved`/`comment.rejected`, `api_token.created`/`api_token.revoked`, `user.account_deleted`, **inviti e membership** (`blog.invitation_created`/`_accepted`/`_declined`/`_revoked`, `blog.member_role_changed`, `blog.member_removed`), **pagine statiche** (`page.created`/`page.updated`/`page.deleted`, di piattaforma e di blog — `blog_id` presente solo per queste ultime). Per evento: **IP sorgente** (`ip`, colonna `INET`), **identità reale dell'attore** al momento dell'evento (`actor_label`, snapshot username+email — mai perso anche se l'account viene poi rinominato/cancellato), e **canale** web/api/system — calcolato da `actor_type` (`user`/`anonymous` = sessione dashboard, `core_token`/`user_token` = accesso diretto via ApiToken, `system` = processo interno), non una colonna a sé ma un campo derivato in `GET /api/v1/admin/audit-log` (`app/api/v1/admin.py::_CHANNEL_BY_ACTOR_TYPE`), filtrabile con `?channel=`. Per gli eventi con un blog di contesto (sospensione, nascondi post, moderazione commenti): `payload.blog_alias`, l'alias pubblico sotto cui compare quel blog (`Blog.default_author_display_name`, `None` se il blog non ne ha uno) — in aggiunta all'identità reale, mai al posto. **Retention e archivio**: `app/workers/audit_maintenance.py` (servizio `worker-audit-maintenance` in `compose.yaml`, `CronJob` `k8s/audit-maintenance.yaml`) archivia su storage (bucket privato `NOCT_S3_BUCKET_AUDIT`, NDJSON gzippato per settimana ISO) le settimane chiuse, registra in `audit_archive_runs`, poi cancella a batch gli eventi oltre `platform_config.audit_retention_days` senza mai superare il watermark di ciò che è archiviato; `--restore AAAAwSS` reimporta una settimana. **Configurabile a runtime** da un Super Admin (`/admin/impostazioni`, `PATCH /api/v1/admin/config`, 7–3650 giorni): `NOCT_AUDIT_RETENTION_DAYS` (default 105) resta solo il seme iniziale usato alla creazione della riga `platform_config`, non più il valore effettivo dopo il primo avvio — richiesta esplicita, non un residuo di ROADMAP.md. **Consultazione**: `GET /api/v1/admin/audit-log` (filtri action/actor/target/blog/canale/intervallo, paginazione) + pannello `frontend/src/app/dashboard/registro` (filtri azione/canale, colonna alias blog). **Corretto in questo blocco** un bug scoperto proprio implementando i nuovi eventi: `target_id` restava sempre `NULL` per `api_token.created` (e sarebbe successo per i nuovi eventi sopra) perché l'id dell'entità — un default Python-side, `uuid.uuid4` — non è popolato sull'oggetto finché non c'è un `flush()`, letto invece subito dopo il solo `session.add()`; corretto con un `await session.flush()` prima di ogni `audit.record()` che referenzia l'id di un'entità appena creata nella stessa funzione. **Manca**: base giuridica GDPR e politica di conservazione degli archivi su storage (oggi illimitata) — decisione di prodotto/legale, non ancora presa, prima di poter implementare una scadenza per gli archivi. **B5**: nota obbligatoria (`payload.note`) per le azioni admin che cambiano stato (utenti, blog, post) e per le azioni dal pannello segnalazioni (`POST /api/v1/admin/blogs/{id}/action`). Segnalazioni dai lettori in `content_reports`. |
| Blog utente su sottodominio | ✅ | Implementato **per slug del blog** (`{blog_slug}.notturni.eu`), non per username: un utente può avere fino a 5 blog (§1), un solo sottodominio possibile a testa via username non avrebbe un modo ovvio di scegliere quale mostrare — lo slug è anche già validato DNS-safe (`blog_rules.py::SLUG_PATTERN`, solo `-`), a differenza dello username che ammette `_` (non valido in un'etichetta DNS). `frontend/src/proxy.ts` (file `proxy.ts`, non `middleware.ts` — convenzione rinominata in questa versione di Next.js) legge l'header `Host` e riscrive internamente `{slug}.notturni.eu/...` su `/{slug}/...`, la stessa route path-based già servita da `[blogSlug]/`; sottodomini riservati (`www`, `blog`, `mail`, ...) non vengono riscritti, restano sulla home di piattaforma. Tutti i link interni generati dalle pagine pubbliche del blog (nav, permalink, bibliografia, media, pubblicazioni) si adattano al contesto corrente via `frontend/src/lib/blog-path.ts::blogLinks` (prefisso `/{slug}` path-based, vuoto su sottodominio) — nessun link "rotto" cliccando da un blog raggiunto via sottodominio. CORS esteso con `NOCT_CORS_ORIGIN_REGEX` (vedi riga Traefik sopra) per le chiamate XHR del frontend dal browser su un sottodominio. **Resta path-based** anche `https://notturni.eu/{blog_slug}/{post_slug}` (permalink canonico, riga dedicata sotto) — il sottodominio è un accesso aggiuntivo, non l'unico. `NOCT_SESSION_COOKIE_DOMAIN`/`NOCT_CORS_ORIGIN_REGEX` restano vuoti nel test senza dominio reale (`k8s/configmap.yaml`), da valorizzare solo con `*.notturni.eu` realmente instradato (vedi riga Traefik). **Corretto in questo blocco**: `/theme-init.js` (script pubblico caricato da `layout.tsx`) veniva riscritto su `/{slug}/theme-init.js` su un sottodominio blog, rompendo l'init del tema — ora escluso dal matcher del proxy come gli altri asset pubblici. |
| Permalink leggibili per i post, senza UUID nell'URL | ✅ | `/{blog_slug}/{post_slug}` — unicità reale su blog+slug+locale (`backend/app/domain/permalinks.py`), endpoint pubblico `GET /blogs/{slug}/posts/{slug}`. **Senza la data** (rimossa: era solo leggibilità/disambiguazione, mai parte dell'unicità — un blog rilancerebbe più facilmente contenuti "senza data" e gli URL restano più brevi e stabili nel tempo); nessun redirect predisposto dal vecchio formato `/{YYYYMMDD}/`, scelta esplicita per un progetto senza ancora backlink reali da preservare. Per evitare che un post finisca "oscurato" da una delle route statiche sotto `/{blog_slug}/...` (bibliografia/media/link/pagina), lo slug di un post non può coincidere con nessuna di queste (`app/domain/permalinks.py::validate_post_slug_not_reserved`, `400` in creazione/traduzione). Pagina pubblica del post lato frontend (Server Component, Markdown renderizzato a HTML e sanificato — vedi `frontend/src/lib/markdown.ts`) su `/{blog_slug}/{post_slug}`, raggiungibile senza passare dal sottodominio del blog. |
| Link "torna al portale" nella top bar | ✅ | Richiesta esplicita: la top bar di un blog (`BlogHeader`) non aveva alcun riferimento verso `notturni.eu` — solo il nome del blog stesso. Nuovo `PlatformMark` (`frontend/src/components/shell/PlatformMark.tsx`): una "N" in un riquadro colorato, riusata sia in `BlogHeader` sia in `SiteHeader` (piattaforma) per coerenza visiva. URL assoluto da `SITE_URL`/`NEXT_PUBLIC_SITE_URL` (non un link relativo `/`): quando arriverà il routing per sottodominio-per-blog, `/` da un blog risolverebbe sul blog stesso, non sulla piattaforma — la env dedicata al FQDN resta corretta in entrambi i casi, oggi e dopo. |
| Footer a 3 colonne + barra inferiore, su ogni pagina pubblica | ✅ | Richiesta esplicita. Contenuto in `platform_config` (`footer_column1/2/3_markdown`, `footer_bottom_bar_markdown`), Markdown libero (immagini/link inclusi) modificabile solo da un Super Admin (`/admin/impostazioni`, sezione Footer, 5000 caratteri max per campo). Mostrato su **ogni** pagina pubblica di piattaforma e di ogni blog: nuovo `frontend/src/components/shell/SiteFooter.tsx` (Server Component asincrono, `GET /api/v1/footer` pubblico, cache a tempo + tag `platform-footer` condiviso), sostituisce la vecchia riga fissa "codice sorgente · fatto in UE" (seminata ora come default del solo `footer_bottom_bar_markdown`, non più hardcoded). **Override per blog**: il proprietario può sovrascrivere le sole colonne 1/2 per il proprio blog in `blog_configs.footer` (`AppearanceTab`, tab Aspetto) — la colonna 3 e la barra inferiore restano sempre e solo di piattaforma, validazione lato backend impedisce di inviarle (`app/domain/blog_config.py`). Nessun footer mostrato se tutto è vuoto (installazione nuova senza colonne configurate), invece di un riquadro vuoto. Rendering con un rendering Markdown "leggero" dedicato (`renderSimpleMarkdown`, `frontend/src/lib/markdown.ts`): niente card di anteprima dei link né avvolgimento immagini sensibili come per i post — non ha senso per un footer mostrato su ogni pagina, eviterebbe fetch di rete ripetuti. `/u/{username}` (Client Component) non può importare direttamente un Server Component asincrono: il footer vi arriva tramite `app/u/[username]/layout.tsx`, già usato per i metadata dello stesso segmento. |
| Homepage pubblica del blog | ✅ | `/{blog_slug}` (`frontend/src/app/[blogSlug]/page.tsx`): titolo, sottotitolo, descrizione ed elenco cronologico dei post pubblicati del blog (riusa `GET /api/v1/blogs/{slug}/posts`, nessun endpoint nuovo — già applicava le regole di visibilità corrette). Risolve anche il link "← {blog}" nella pagina del post, che prima puntava qui e dava `404` (nessuna route esisteva). **Nota**: come le altre pagine pubbliche renderizzate server-side, non inoltra un'eventuale sessione dell'utente — un blog `members`/`private` risulta quindi `404` anche per un visitatore autenticato (limite architetturale preesistente, non introdotto qui). **Layout personalizzabile dall'utente**: non ancora — oggi è un unico layout fisso, un possibile passo successivo quando si affronterà la personalizzazione di `blog_configs` lato rendering pubblico (vedi nota nella riga "Personalizzazione colori/tipografia/presentazione per blog"). **Audit link interni** (dopo la ristrutturazione degli URL sopra — `/admin/*`, `/p/{slug}`, permalink senza data): nessun altro riferimento residuo ai vecchi percorsi (`/dashboard/pagine`, `/dashboard/utenti`, `/dashboard/moderazione*`, `/dashboard/registro`, `/pages/{slug}`, permalink a 3 segmenti) in tutto `frontend/src/app`/`components`, né link a funzionalità mai costruite — non servono pagine segnaposto. |
| `notturni.eu`: raccolta articoli nella lingua dell'utente (stile dev.to) | ✅ | Homepage con feed cronologico di tutti i blog (`GET /api/v1/feed/posts`, filtro `locale`/`tag` opzionale) e sezione "di tendenza" per tag più usati negli ultimi 7 giorni (`GET /api/v1/feed/trending`) — vedi riga "Tag sui post" sotto. |
| Feed RSS/Atom, di piattaforma e per blog | ✅ | Richiesta esplicita. Route Next.js (`frontend/src/app/feed.xml`, `.../atom.xml`, `.../[blogSlug]/feed.xml`, `.../[blogSlug]/atom.xml`): nessun endpoint backend nuovo, riusano `GET /feed/posts`/`GET /blogs/{slug}/posts` già filtrati per visibilità. Solo titolo + estratto per voce (`frontend/src/lib/feed.ts`), mai il contenuto completo: un'immagine segnalata sensibile è sfocata via CSS sulla pagina, un trucco che un lettore di feed non applica — l'estratto resta testo puro. Autodiscovery (`<link rel="alternate">`) e link "RSS" visibile sulla home di piattaforma e su quella di ogni blog. `RESERVED_POST_SLUGS` (`backend/app/domain/permalinks.py`) estesa con `feed.xml`/`atom.xml` (e `pub`, mancante da B9 — bug residuo corretto qui) per evitare che uno slug di post collida con queste route statiche. |
| Filtro lingua della homepage limitato alle lingue realmente in uso | ✅ | `GET /api/v1/feed/locales` (nuovo, accanto a `GET /feed/trending`): conteggio post per lingua con gli stessi filtri di visibilità di `GET /feed/posts`, `GROUP BY`/`ORDER BY count DESC`, massimo 5 risultati — sostituisce l'elenco statico hardcoded (`it`/`en`/`de`/`fr`) usato finora sulla homepage, così compaiono solo le lingue con almeno un post pubblicato, mai più di 5. **Corretto in questo blocco**: la riga dei pill lingua (`frontend/src/app/page.tsx`) usava `overflow-x-auto` senza `flex-wrap`, tagliando visivamente l'ultimo pill/il link RSS quando lo spazio non bastava — ora `flex-wrap`, i pill vanno a capo. |
| Ricerca globale (portale) e per singolo blog (anche da sottodominio) | ✅ | Due endpoint distinti (`GET /api/v1/search/posts` cross-blog, `GET /blogs/{slug}/search` ristretto a un blog): `ILIKE` su titolo/contenuto, stesso approccio pragmatico già in uso per `GET /blogs?q=` (nessuna infrastruttura full text search Postgres/`tsvector`/`pg_trgm` — da valutare solo se le prestazioni lo richiederanno a scala). Stessi vincoli di visibilità pubblica del feed/di `GET /blogs/{slug}/posts`; la ricerca per blog mostra solo post effettivamente pubblicati anche a chi ha accesso in scrittura (non è uno strumento di editing). Frontend: `/search` (icona ⌕ in `SiteHeader`) e `/{blogSlug}/search` (voce "Cerca" in `BlogHeader`, raggiungibile anche da `{slug}.notturni.eu/search` — non è tra i `PLATFORM_ONLY_PATHS` di `proxy.ts`), entrambe form GET semplice (`?q=`) come già `/blogs`. Per cercare i *blog* per nome resta `GET /blogs?q=` (già esistente, non duplicato qui). **Corretto in questo blocco**: `GET /api/v1/search/posts` e tutto `feed.py` (`/feed/posts`, `/feed/locales`, `/feed/trending`) filtravano la visibilità con una condizione SQL grezza (`published_at <= now()`) diversa dall'helper canonico `publicly_visible_clause()` già usato da `posts.py`/`bibliography.py`/`seo.py` — quest'ultimo tratta un post pubblicato con `published_at NULL` come visibile, la condizione grezza lo escludeva (in SQL `NULL <= now()` è falso). Allineati tutti alla stessa clausola. |
| Newsletter per blog (doppio opt-in) e digest di piattaforma | ✅ | Tabelle `newsletter_subscribers`/`newsletter_campaigns` (`backend/app/models/newsletter.py`, un iscritto per email per lista via indici unique parziali su `blog_id`/`email`, `blog_id NULL` = digest di piattaforma). **Iscrizione pubblica** senza account (`POST /api/v1/newsletter/subscribe`, `email`+`blog_slug` opzionale), rate-limited per IP e per email (`app/domain/rate_limit.py`), risposta sempre generica (anti-enumerazione). **Doppio opt-in**: token opaco hashato (stesso schema di `api_tokens`) inviato via coda RabbitMQ dedicata (`NEWSLETTER_SEND_QUEUE`, `app/core/broker.py`) e worker `app/workers/newsletter_consumer.py` (stesso stile di `email_otp_consumer.py`, SMTP via `app/core/mail.py`); conferma su `GET /api/v1/newsletter/confirm` (idempotente). **Disiscrizione e cancellazione GDPR (Art. 17)** senza login, via link firmato HMAC (non un hash in tabella: il worker deve poter ricostruire il link ad ogni invio) — `POST /newsletter/unsubscribe` (con motivo opzionale) e `POST /newsletter/unsubscribe/delete` (cancellazione permanente della riga). Le righe `unsubscribed` non vengono cancellate automaticamente (soppressione contro re-iscrizioni senza nuovo consenso); l'export/cancellazione account (`app/domain/gdpr.py`) include/cancella le iscrizioni collegate a un utente loggato. **Invii**: notifica automatica ad ogni post pubblicato (`Blog.newsletter_auto_notify_enabled`, disattivabile per blog, hook nella stessa transazione della pubblicazione in `app/api/v1/posts.py`, un solo invio per post via indice unique parziale su `post_id`) e campagne manuali composte da chi ha ruolo Autore/Co-Autore (`can_manage_newsletter`, stessa logica di `can_write_posts`) o da un Super Admin/Amministratore per il digest di piattaforma (`GET/POST /api/v1/blogs/{slug}/newsletter/campaigns`, `.../stats`, `PATCH .../settings`; equivalenti admin sotto `/api/v1/admin/newsletter/*`). Frontend: form di iscrizione inline (non modal — sezione di pagina, non un'azione a scomparsa) in home page (digest) e nella home di ogni blog (`NewsletterSignup.tsx`), pagine dedicate `/newsletter/confirm` e `/newsletter/unsubscribe` (mai un side-effect su semplice `GET`, sempre un click esplicito — un client email che precarica i link non deve poter disiscrivere nessuno), tab "Newsletter" nella dashboard del blog (contatori, toggle invio automatico, elenco campagne, composer in modal `NewsletterCampaignModal.tsx`) e sezione digest in `/admin/newsletter`. **Manca**: uno scheduler che invii davvero le campagne create con `scheduled_at` futuro — oggi restano in stato `scheduled` senza nulla che le faccia scattare; export CSV degli iscritti per l'autore. Verificato dal vivo su Podman compose (iscrizione → email di conferma su mailhog → conferma → campagna digest inviata → link di disiscrizione e di cancellazione dati funzionanti dall'email reale → notifica automatica alla pubblicazione di un post con permalink corretto). **Bug corretto in questo blocco, trovato solo dalla verifica dal vivo** (non dai test, che usano sessioni mockate): `newsletter_consumer.py` apriva un nuovo event loop asyncio (`asyncio.run(...)`) per ogni messaggio pur riusando lo stesso `AsyncEngine` a livello di modulo (`app/core/database.py`) — le connessioni asyncpg pooled restano legate al loop che le ha aperte, quindi il messaggio successivo trovava connessioni orfane e andava in loop infinito di nack/requeue (`InterfaceError: another operation is in progress`, centinaia di redelivery al secondo). Corretto riusando un solo event loop per l'intera vita del processo (`loop.run_until_complete` invece di `asyncio.run`, `main()`); aggiunto anche un `session.rollback()` mancante nel percorso di gestione errore prima di riusare la sessione per segnare la campagna `failed`. **Corretto in questo blocco**: `_process_campaign` non era idempotente su ridelivery del messaggio (nack/requeue dopo un crash a metà invio, o dopo un invio già completato con successo) — rispediva l'email a *tutti* i destinatari da capo, inclusi quelli già notificati. Aggiunto `NewsletterCampaign.sent_to_subscriber_ids` (aggiornato e committato dopo ogni singolo invio riuscito, non solo a fine ciclo) e un controllo esplicito su `status == SENT` in testa alla funzione. Anche `scheduled_at` non richiedeva un datetime timezone-aware: un valore naive sollevava `TypeError` invece di un 422 leggibile al confronto con l'istante corrente — validato con `field_validator` su `CampaignCreateRequest`. Entrambi verificati dal vivo (naive → 422, aware → 201; vedi anche la riga cambio password per lo stesso pattern di verifica). **Aggiunto in questo blocco**: configurazione delle email di campagna — `GET`/`PATCH .../newsletter/settings` (tri-state, campo omesso invariato/`null` azzera) espone anche `newsletter_sender_name` (header `From`, indirizzo resta sempre `NOCT_SMTP_FROM_EMAIL`) e `newsletter_banner_url`/`newsletter_banner_alt_text` (banner in cima al corpo), con l'equivalente `/admin/newsletter/settings` per il digest di piattaforma (sorgente `platform_config`, nessun `newsletter_auto_notify_enabled`). Le email di campagna sono ora `multipart/alternative` (`app/core/mail.py::send_email`, nuovi parametri opzionali `html_body`/`from_name` — gli altri invii di piattaforma, OTP e reset password, restano solo testo): fallback testuale invariato più una versione HTML con banner, paragrafi e URL nel corpo autolinkati. UI: nuovo `NewsletterSettingsCard.tsx` (nome mittente, banner con upload diretto per un blog tramite la libreria media già esistente, URL incollato per il digest di piattaforma che non ha un blog a cui associare l'upload), condiviso tra la tab Newsletter del blog e `/admin/newsletter`. Verificato dal vivo: campagna inviata con banner e nome mittente personalizzati, email HTML ricevuta su mailhog con `From: Redazione Prova <...>`, `<img>` del banner con alt text e link "Leggi tutto"/URL nel corpo cliccabili. **Aggiunto in questo blocco**: una campagna manuale ancora `scheduled` può ora essere modificata (`PATCH .../campaigns/{id}`: oggetto, corpo, data — tri-state su `scheduled_at`, `null` o un istante nel passato la invia subito) o annullata (`DELETE .../campaigns/{id}`, la porta a `status=canceled` senza cancellare la riga), sia per un blog sia per il digest di piattaforma. Non più possibile mentre `status=sending` (409: il worker può già averla presa in carico) — le automatiche `post_notification` nascono già `sending`, quindi mai in questo stato. UI: azioni "Modifica"/"Annulla" sulle campagne pianificate in `NewsletterTab.tsx` e `/admin/newsletter`, `NewsletterCampaignModal.tsx` ora riusato anche in modalità modifica. Verificato dal vivo: modifica dell'oggetto e annullo di una campagna pianificata, 409 su una campagna già `sending`/`canceled`. **Aggiunto in questo blocco**: il corpo della campagna (`NewsletterCampaignModal.tsx`) usa ora lo stesso `RichTextEditor` dei post (`RichTextEditorLazy`, `stickyToolbar={false}` per il modal) invece di un `<textarea>` di markdown grezzo — per un blog abilita anche l'immagine incorporata e l'upload sulla libreria media del blog, disattivati per il digest di piattaforma (nessun blog a cui associarli). Il corpo viene ora renderizzato davvero a HTML nell'email (nuovo `app/domain/markdown_render.py`, `markdown` + sanificazione `nh3`), non più solo escaped con autolink; la notifica automatica costruisce anche una sorgente Markdown propria (`### {titolo}` + estratto + `[Leggi tutto](url)`) distinta dal corpo testuale semplice del fallback. Nuove dipendenze `markdown`/`nh3` in `backend/requirements.txt`. Verificato dal vivo: campagna con titolo/grassetto/elenco puntato/link renderizzati correttamente in HTML (heading, `<strong>`, `<ul>`, `<a>`), notifica automatica con link "Leggi tutto" cliccabile. |
| Tag sui post (massimo 5, da campo dedicato e/o `#hashtag` nel testo) | ✅ | `app/domain/tags.py` + tabelle `tags`/`post_tags`; oltre il limite: errore esplicito, mai troncamento silenzioso. Usati per il filtro `?tag=` del feed e per la sezione "di tendenza" della homepage. |
| Categorie sui post, oltre ai tag | ✅ | Tassonomia per-blog (`categories`, `posts.category_id`), a differenza dei tag definita in anticipo da proprietario/autori e al più una per post. CRUD in `POST/blogs/{slug}/categories`; filtro `?category=` anche sul feed della homepage. |
| Pubblicazioni: serie di articoli ordinati cronologicamente (stile libro/saggio) | ✅ | Tabella `publications` + `Post.publication_id`/`chapter_order` (todo/PUBLICATIONS.md, todo/UX_REDESIGN.md B9): un post appartiene al più a una pubblicazione, indice pubblico `/{blog}/pub/{nome}` in ordine cronologico dal più vecchio o esplicito (drag-to-order in dashboard), capitolo con avanzamento e precedente/successivo, `GET/POST/PATCH/DELETE /api/v1/blogs/{slug}/publications` e `PUT .../order`. |
| Viste aggregate multi-blog nella dashboard (Post/Media/Pubblicazioni/Link/Bibliografia) | ✅ | Nuove voci nella sidebar della dashboard, subito dopo "I miei blog": ognuna elenca l'entità su **tutti** i blog di cui l'utente è proprietario o membro (nuovo helper `app/domain/blog_scope.py::my_blog_ids`, union tra `Blog.owner_id` e `BlogMembership.user_id`, riusato da tutti e 5 gli endpoint), non un solo blog per volta come le tab esistenti in `dashboard/blogs/{slug}` (che restano invariate). Nuovi endpoint di sola lettura `GET /api/v1/users/me/{posts,media,publications,links,bibliography}`, stesso principio di attribuzione già usato in `/dashboard/frammenti` (`blog_slug`/`blog_title` per elemento). `links`/`bibliography` sono viste da autore (includono anche contenuto non pubblico), a differenza delle omonime pagine pubbliche `/{blog}/link`/`/{blog}/bibliografia` che applicano `publicly_visible_clause()`. Frontend: `dashboard/{post,media,pubblicazioni,link,bibliografia}`, sola lettura (modifica/upload restano nelle tab per-singolo-blog). |
| Selettore lingua dell'interfaccia nella sidebar della dashboard | ✅ | `UiLanguagePicker` (già usato in `SiteHeader`/profilo) aggiunto anche nel footer della sidebar dashboard, subito prima del pulsante di logout — prima era raggiungibile solo dall'header di piattaforma o dalla pagina profilo, non dalla dashboard stessa. |
| `blog.notturni.eu`: blog ufficiale della piattaforma | ⚪ | Non implementato (nessuna distinzione oggi tra blog "di piattaforma" e blog utente). |
| Blocco nome blog: minimo 4 caratteri alfanumerici (`-` ammesso) | ✅ | `backend/app/domain/blog_rules.py`. |
| Nomi di blog di 3 caratteri o meno riservati alla piattaforma | ✅ | Idem — bloccati oggi, nessuna funzionalità premium di sblocco ancora costruita. |
| Blacklist nomi riservati (blog, www, mail, admin, ...) | ✅ | `backend/app/domain/blog_rules.py`, più blacklist separata per gli username (`backend/app/domain/usernames.py`). **Corretto in questo blocco**: `search`/`users` mancavano da entrambe le liste (oltre che da `RESERVED_POST_SLUGS` in `permalinks.py` e da `RESERVED_SUBDOMAINS` in `frontend/src/proxy.ts`) — sotto `[blogSlug]/` "search" è già una route statica sibling di `[postSlug]`, un post con quello slug sarebbe rimasto irraggiungibile; aggiunte a tutte e quattro le liste (tenute allineate per commento esplicito nel codice). |
| cert-manager / Let's Encrypt in produzione | ✅ | `k8s/cert-manager-issuer.yaml`: due `ClusterIssuer` (`letsencrypt-staging`/`letsencrypt-prod`), solver DNS-01 su Cloudflare (`apiTokenSecretRef` verso `NOCT_CLOUDFLARE_API_TOKEN` in `secret.yaml`, permesso minimo Zone:DNS:Edit sulla sola zona) — necessario per il wildcard `*.notturni.eu`, la sfida HTTP-01 non lo copre. `k8s/certificate.yaml`: una `Certificate` esplicita per `notturni.eu` **e** `*.notturni.eu` insieme (un solo Secret TLS `notturni-eu-tls`, condiviso da `ingress.yaml`/`ingressroute.yaml` — non l'annotazione per-Ingress, che non copre la CRD `IngressRoute` di Traefik). `NOCT_SESSION_COOKIE_SECURE` portato a `"true"` in `configmap.yaml` insieme al passaggio a TLS. cert-manager stesso resta un prerequisito esterno (Helm), non incluso nei manifest — stesso principio di Longhorn/Traefik, istruzioni in `k8s/README.md`. **Non verificato dal vivo** (nessun cluster K3s raggiungibile in questa sessione): sintassi validata con `kubectl kustomize .`, l'emissione reale del certificato resta da fare sul nodo reale (raccomandato prima con `letsencrypt-staging`, rate limit di produzione stretti). |
| DNS wildcard `*.notturni.eu` | ✅ | Prerequisito infrastrutturale esterno al repository — l'utente lo ha già configurato su Cloudflare, puntato al nodo K3s di destinazione. |
| CI: SAST, DAST e SBOM | 🟡 | Tre GitHub Actions in `.github/workflows/`: **SAST** (`codeql.yml`, CodeQL su backend/moderation Python + frontend TypeScript/JavaScript, report nella tab Security → Code scanning); **SBOM** (`sbom.yml`, CycloneDX via `anchore/sbom-action` — un livello dai sorgenti per sotto-progetto e uno dalle immagini container backend/frontend effettivamente costruite, `moderation` escluso da quest'ultimo per il peso dell'immagine ML/torch; report come artifact); **DAST** (`dast.yml`, OWASP ZAP baseline sul frontend + API scan sul backend da `/openapi.json`, stack avviato da `compose.yaml` con Docker al posto di Podman — nessuna sintassi specifica di Podman nel file). CodeQL/SBOM su push/PR verso `main`/`develop` + avvio manuale + schedule settimanale; DAST solo avvio manuale + schedule settimanale (più pesante: build completa + avvio stack + scansione). Nessuno dei tre fa fallire la build in presenza di segnalazioni (`fail_action: false`): pensati per produrre un report da consultare, non un gate di merge. **Manca**: una soglia/policy che promuova un sottoinsieme di segnalazioni a bloccante; scansione delle vulnerabilità note sulla SBOM (oggi è solo inventario, non analisi CVE); SBOM/immagine per `moderation`. **Corrette in questo blocco** 6 segnalazioni CodeQL: SSRF completa su `link_preview.py` (pinning IP sull'indirizzo verificato invece di lasciare che l'HTTP client ri-risolva l'hostname al momento della richiesta — altrimenti un DNS rebinding fra le due risoluzioni vanifica la validazione fatta prima), 2 ReDoS polinomiali confermati empiricamente (`blog_notes.py::_ENTRY_RE`, gruppi atomici; `content_media.py::_IMAGE_RE`/`_LINK_RE`, riscritta senza ambiguità e con supporto a URL con parentesi bilanciate tipo Wikipedia — da ~45s a sub-lineare su un input patologico da 32.000 ripetizioni), sanitizzazione URL incompleta su un controllo per substring in `blog_notes.py::guess_kind` (ora sull'host effettivo via `urlparse`), e un limite di lunghezza esplicito sull'import BibTeX (`MAX_BIBTEX_LENGTH`, difesa in profondità anche a prescindere dalla regex). Un sesto avviso (log in chiaro del token in `scripts/create_api_token.py`) è un falso positivo — è l'unico modo in cui lo script di bootstrap consegna il token a chi lo esegue — silenziato con un commento di soppressione motivato invece che rimosso. |
| Migrazione progressiva dei microservizi a Rust (orizzonte 3 anni) | ⚪ | Non iniziata, coerente con la roadmap di lungo periodo. |

## 4. Autenticazione e SSO

| Specifica | Stato | Note / task residuo |
|---|---|---|
| Login password + sessione JWT (access + refresh con rotation) | ✅ | |
| Policy minima password | ✅ | `app/domain/passwords.py::validate_password_policy` — unico requisito: minimo 10 caratteri (`PASSWORD_MIN_LENGTH`), nessun vincolo su classi di caratteri. Applicata in registrazione e nel reset password sotto (nessun altro endpoint cambia la password oggi). Frontend: `minLength={10}` su registrazione e reset, hint aggiornato. |
| Verifica disponibilità username in fase di registrazione | ✅ | `GET /api/v1/auth/username-available?username=...`, pubblico, rate-limited (30/min per IP). Riusa `validate_username` (`app/domain/usernames.py`, formato/parole riservate) e la stessa query di unicità di `register_user` — risposta `{available, reason}` (`reason` = `invalid_format`\|`taken`\|`null`). Frontend: controllo debounced (300ms, stesso pattern di `SearchInput`) sul campo username di `/register`, submit bloccato se non disponibile. |
| Cambio password da loggati (vecchia + nuova password) | ✅ | `POST /api/v1/users/me/password`, autenticato, body `{current_password, new_password}`. Verifica la password attuale (`verify_password`, stessa funzione del login), applica la policy minima, **revoca tutte le sessioni attive** — stesso principio di sicurezza del reset via email sotto (`app/domain/password_reset.py`), qui replicato esplicitamente perché nessun endpoint di cambio password autenticato esisteva finora (solo il reset via OTP, senza vecchia password). `400` se l'account non ha una password impostata (solo SSO). Frontend: nuova sezione "Cambia password" in `dashboard/profile` (`#sicurezza`, accanto a MFA); dato che il backend chiude anche la sessione corrente, dopo il successo la UI fa logout e reindirizza a `/login`. **Corretto in questo blocco** (stesso gap anche nel reset "password dimenticata"): la revoca cancellava solo le `UserSession` (refresh token), ma l'access token JWT già emesso restava valido — stateless, nessun controllo a DB — fino al suo `exp` naturale (15 minuti). Nuovo campo `User.credentials_changed_at`, confrontato con l'`iat` del token in `get_current_user`/`get_optional_current_user`: un token emesso prima dell'ultimo cambio password è ora rifiutato (401) anche se non ancora scaduto. Verificato dal vivo: stesso access token, 200 prima del cambio password e 401 subito dopo. |
| Password dimenticata (reset via email) | ✅ | Stesso pattern OTP dell'MFA email/cambio email (codice a 6 cifre, hash sha256, TTL 10 minuti, tabella dedicata `password_reset_codes`): `POST /api/v1/auth/password/forgot` (sempre `202`, email enumerabile altrimenti) e `POST /api/v1/auth/password/reset` (verifica codice + policy minima, **revoca tutte le sessioni attive** dell'utente — un reset è tipicamente una risposta a un account compromesso). Rate limit 20/5 min per IP, 5/5 min per email su entrambi. Frontend: `/forgot-password`, linkata dal form di login. Verificato dal vivo end-to-end (mailhog): codice ricevuto via email, reset applicato, vecchia password non più valida, nuova funzionante. **Corretto in questo blocco**: `OtpInput` (condiviso con il passo MFA del login) aveva legend/aria-label delle 6 cifre fissi in italiano — ora passati dal chiamante via `next-intl`, corretti anche nel flusso in inglese. |
| Reset forzoso della password da amministrazione | ✅ | `POST /api/v1/admin/users/{user_id}/reset-password` (Amministratore/Super Admin, `202`): innesca verso l'utente lo stesso ciclo email della riga sopra — riusa direttamente `app/domain/password_reset.py::request_password_reset`, nessuna duplicazione di logica invio/DB. L'admin non imposta né vede alcuna password, solo avvia l'invio; `400` se l'utente target non è attivo. Nessun rate limit IP/email del self-service (pensato per un anonimo che enumera indirizzi): un limite separato più permissivo per attore admin (20/5 min) protegge solo da un account admin compromesso. Evento di audit `user.password_reset_triggered`, senza nota obbligatoria (non è un cambio di stato persistente sull'account, a differenza di cambio ruolo/attivazione). Frontend: bottone "Reset password" per riga in `/admin/utenti` con conferma inline (Sì/No, non un modal a sé) e riscontro "Email inviata". |
| MFA TOTP (app authenticator) | ✅ | Via `pyotp`. Setup con **QR code** generato interamente lato backend come SVG inline (`qrcode`, `app/domain/mfa.py::totp_qr_code_data_uri` — nessuna dipendenza da Pillow, solo l'image factory SVG; nessun servizio di generazione QR di terze parti: il secret non deve mai lasciarlo), restituito come data URI da `POST /api/v1/auth/mfa/totp/setup` insieme al secret in chiaro (rimasto disponibile per l'inserimento manuale, dietro un dettaglio a scomparsa). |
| MFA Email OTP asincrono | ✅ | Accodato su RabbitMQ, il consumer (`app/workers/email_otp_consumer.py`) invia davvero il codice via SMTP (`app/core/mail.py`, solo stdlib `smtplib`) con `NOCT_SMTP_HOST`/`_PORT`/`_USER`/`_PASSWORD`/`_USE_TLS`/`_FROM_EMAIL`. In locale punta al servizio `mailhog` di `compose.yaml` (nessuna email reale, ispezionabile su `http://localhost:8025`); senza `NOCT_SMTP_HOST` configurato l'OTP resta solo loggato (comodo in sviluppo, mai il caso in produzione). Un errore SMTP genuino fa nack/requeue del messaggio, come il consumer del backup post. |
| SSO Google/Microsoft/GitHub/LinkedIn (OAuth2/OIDC via Authlib) | 🟡 | Implementato a livello di codice (backend); **non testabile end-to-end** senza credenziali OAuth reali per ciascun provider — verificato solo a livello di logica di dominio. **Decisione precedente superata in questo blocco, richiesta esplicita**: i pulsanti SSO sono ora mostrati in `/login` e `/register` (`frontend/src/components/auth/SsoButtons.tsx`) — non più "nessun pulsante finché non testato end-to-end", ma condizionati a `GET /api/v1/config::sso_providers` (già l'intersezione tra provider configurati via env e quelli abilitati a livello di piattaforma, `PATCH /admin/config`): un'istanza senza alcun provider configurato non mostra nulla, nessun placeholder. Ogni pulsante è un link `<a>` (mai una fetch XHR: il flow OAuth2/OIDC richiede una navigazione vera del browser) verso `GET /auth/sso/{provider}/login`, redirect già esistente lato backend. **Resta non testato end-to-end** con credenziali OAuth reali (nessuna disponibile in questo ambiente di sviluppo) — solo verificato che il pulsante compare/non compare correttamente in base a `sso_providers` e che l'URL generato è quello giusto. |
| Account linking basato su email, con gate 2FA | ✅ | |
| Token API (`api_tokens`, prefisso `noct_`, hash sha256) | ✅ | Token "core" (motore, machine-to-machine) e "utente" pieni: `POST/GET/DELETE /api/v1/tokens` accettano sia un ApiToken opaco esistente (bootstrap/rotazione core) sia una sessione JWT (per il *primo* token utente, dalla dashboard — `app/api/deps.py::get_token_actor`). UI in `frontend/src/app/dashboard/token`. |
| GDPR: MFA/2FA come rafforzamento sicurezza account | ✅ | Vedi sopra; nessuna funzionalità GDPR-specifica oltre a questo (vedi riga dedicata in [1](#1-prodotto-e-regole-di-dominio)). |
| Sessione in `localStorage` | ✅ | Sostituita: il refresh token vive solo in un cookie `httpOnly` + `Secure` + `SameSite` impostato dal backend (`backend/app/api/v1/auth.py`), mai nel corpo JSON né in `localStorage`. L'access token (15 min) resta nel corpo della risposta ma il frontend lo tiene solo in memoria (`frontend/src/lib/auth-context.tsx`) — un reload completo richiede un refresh silenzioso all'avvio, già gestito. Protezione CSRF (double-submit `noct_csrf_token` + header `X-CSRF-Token`) sui soli endpoint autenticati dal cookie (`/auth/refresh`, `/auth/logout`); il resto delle scritture resta su `Authorization: Bearer`, già immune a CSRF. Attributi del cookie configurabili via `NOCT_SESSION_COOKIE_SECURE`/`_SAMESITE`/`_DOMAIN` (`.env.example`, `k8s/configmap.yaml`) — pensato per non bloccare un rollout di test su K3s senza TLS ancora attivo: `SECURE=false` lì, `true` (default) non appena servito in https. Dettagli: `backend/API.md`. |

## 5. Sviluppi futuri (non ancora iniziati)

Elencati nel blueprint come esplicitamente fuori dall'ambito attuale. Restano qui
come backlog, nell'ordine di priorità indicato:

| Specifica | Stato | Note |
|---|---|---|
| Federazione AT Protocol (Bluesky) | ⚪ | Priorità 1. Unico prerequisito già presente: UUID come chiave primaria su tutte le entità. |
| Federazione ActivityPub (Mastodon) | ⚪ | Priorità 2, dopo AT Protocol. Stack di federazione incompatibile con AT Protocol: richiederà un'implementazione separata. |
| Conteggio like, citazioni, condivisioni | ⚪ | |
| Dominio custom dell'utente associato al proprio blog | ⚪ | Dipende dal lavoro di routing per sottodominio (vedi [3](#3-architettura-stack-e-infrastruttura)). **Diverso** dal dominio personalizzato di profilo verificato via DNS già implementato ([§1](#1-prodotto-e-regole-di-dominio), riga "Dominio personalizzato verificato via DNS + badge di verifica"): quello è un'identità mostrata sul profilo, non cablata nel routing — un blog non è ancora raggiungibile dal dominio custom di un utente. |

## 6. Ottimizzazioni ed erogazione

Blocco aperto dopo l'analisi della codebase del branch `feat/code-optimization`
(5 settembre 2026): interventi mirati a snellire il codice, ridurre il costo di
erogazione delle pagine pubbliche e migliorare l'esperienza utente. Non
introducono funzionalità nuove — sono debito tecnico e prestazioni.

| Intervento | Stato | Note / dettaglio |
|---|---|---|
| Indici DB su relazioni calde del feed | ✅ | `comments.post_id`, `user_follows.followed_user_id`, `blog_follows.blog_id`, `posts.author_id`, `posts.category_id` erano senza indice; aggiunto anche l'indice composito `(status, published_at)` su `posts` per l'ordinamento del feed e delle tendenze. `posts.blog_id` è già coperto dallo `UniqueConstraint(blog_id, slug, locale)`. Modelli + migrazione `b1c2d3e4f5a6`. |
| Eliminazione N+1 in `_post_out` | ✅ | `backend/app/api/v1/posts.py`: la serializzazione di un elenco di post (feed della homepage, elenco post di un blog) faceva ~3-4 query per post in sequenza (note, autore, categoria, membership). Introdotto `_posts_out()` che carica tutto in 4 query batch (`IN (…)`); `_post_out()` singolo ne è ora un caso particolare. Usato da `feed.py::list_feed` e `posts.py::list_posts`. |
| Filtri di visibilità post spinti in SQL | ✅ | `list_posts` e `list_post_translations` filtravano `is_publicly_visible` in Python dopo aver caricato tutte le righe. Aggiunto `publicly_visible_clause()` in `backend/app/domain/authorization.py` (equivalente SQL: `not is_hidden AND status='published' AND (published_at IS NULL OR published_at <= now())`), applicato nella `WHERE` per chi non ha accesso in scrittura. `get_post_by_permalink` risolve già un solo post e non è toccato. |
| Cache dei Server Component pubblici | ✅ | I fetch pubblici in `frontend/src/lib/server-api.ts` sono passati da `cache: "no-store"` a `next: { revalidate: 60, tags: [...] }` (tag centralizzati in `frontend/src/lib/revalidate.ts`). Invalidazione on-demand: route handler `frontend/src/app/api/revalidate` (secret condiviso `NOCT_REVALIDATE_SECRET`) chiamata fire-and-forget dal backend (`backend/app/core/revalidation.py`, timeout 2s, mai bloccante) dopo publish/update/traduzione di un post, config e impostazioni di un blog, pagine statiche di blog e di piattaforma, e sospensione blog / nascondi post da admin. Env: `NOCT_FRONTEND_REVALIDATE_URL` + `NOCT_REVALIDATE_SECRET` (compose.yaml, k8s configmap/secret, `.env.example`). Senza secret configurato tutto degrada alla sola rivalidazione a 60s. Il guadagno è sui fetch dati (backend/DB), inclusa ora `resolveLinkCards` (vedi riga sotto): tutte le pagine pubbliche ne beneficiano allo stesso modo. |
| Pipeline `renderMarkdown`: un solo parse DOM | ✅ | `frontend/src/lib/markdown.ts` faceva `new JSDOM(...)` + ri-serializzazione in ognuno dei 4 passi (immagini sensibili, card dei link, menzioni, note): ora `renderMarkdown` fa **un solo parse** e le quattro funzioni mutano lo stesso `document` in place (firma `(document: Document) => void`). Aggiunto `AbortSignal.timeout(2000)` alla fetch di `resolveLinkCards`: un sito esterno lento non blocca più il rendering della pagina, la card degrada a hostname/URL. Il `cache: "no-store"` di quella fetch è passato a `next: { revalidate: REVALIDATE_SECONDS }` (stessa finestra delle altre fetch pubbliche) ora che `GET /api/v1/link-preview` ha una propria cache lato backend (Redis + tabella dedicata, vedi [§1](#1-prodotto-e-regole-di-dominio) "Editor pulito"): la pagina pubblica del post con almeno una card di link non resta più `ƒ` (dynamic) solo per questo. |
| Split di `blogs.py` (1323 righe) e `dashboard/blogs/[slug]/page.tsx` (918 righe) | ✅ | Nessun cambio d'API. Backend: `blogs.py` è ora il package `backend/app/api/v1/blogs/` — un `router` unico condiviso (`_router.py`), schemi/helper comuni in `_common.py`, e un modulo per risorsa: `crud`, `config`, `media`, `categories`, `bibliography`, `pages`, `invitations` (ricevuti + lato proprietario), `members`. L'ordine di import in `__init__.py` conta solo per `invitations` (prima di `crud`, così `GET /received-invitations` non viene catturato da `GET /{slug}`). Frontend: la pagina è la sola shell (~100 righe), i tab sono in `frontend/src/components/dashboard/blog/*` (`PostsTab`, `PagesTab`, `CommentsTab`, `CollaboratorsTab`, `AppearanceTab`, `SettingsTab` + `MyMembershipCard`), con `errorMessage`/`ROLE_LABELS` in `shared.ts`. Un hook fetch/loading/error condiviso resta un possibile passo successivo, non fatto qui. |
| Endpoint commenti per-blog aggregato | ✅ | `GET /api/v1/blogs/{blog_slug}/comments?status=pending` (`backend/app/api/v1/comments.py`): commenti di tutti i post del blog nello stato indicato (`pending` default), dal più recente, ciascuno con `post_title`/`post_slug`; una query con join Comment→Post + risoluzione display-name in blocco. Autorizzazione come `/comments/pending` (proprietario o mediatore). `CommentsTab` nel dashboard ora fa una sola richiesta invece dell'N+1 (`api.posts.list` + una fetch per post). La versione **cross-blog** per l'admin/moderatore di piattaforma è `GET /api/v1/admin/comments`, vedi [1](#1-prodotto-e-regole-di-dominio). |
| Eliminazione N+1 in `GET /posts/{id}/comments` (approvati e in coda) | ✅ | `backend/app/api/v1/comments.py::_comment_out` risolveva l'autore registrato di ogni commento con un `session.get(User, ...)` in un loop Python (una query per commento). Introdotto `_comments_out_batch()` — stesso principio di `_posts_out`/`list_blog_comments`: un solo `SELECT ... IN` per tutti gli autori della lista. Usato da `list_approved_comments` (pubblico, sotto il post) e `list_pending_comments` (coda di moderazione). |
| `/u/{username}` da Client Component a Server Component | ✅ | Vedi [§1](#1-prodotto-e-regole-di-dominio) riga "Ottimizzazione SEO": era invisibile ai crawler senza esecuzione JS (dati caricati via `useEffect` dopo il mount, HTML iniziale vuoto). Ora Server Component, dati risolti server-side; resta client-side solo `FollowUserButton` (sessione del visitatore). |
| `next/image` sui componenti pubblici con copertina | ✅ | `FeedPostCard`/`FeedPostCardClient` (feed piattaforma e "Seguiti") e `SensitiveImage` (copertina di blog/post) usavano `<img>` semplice: niente lazy-loading nativo garantito, niente prevenzione del layout shift via dimensioni esplicite. Passati a `next/image` con `fill` + contenitore `relative` a aspect-ratio fissato; `unoptimized` (stesso pattern già in uso per l'avatar del profilo) perché l'host di storage è configurabile via env (S3/MinIO/R2, CLAUDE.md §3) e non elencabile in anticipo in `images.remotePatterns`. Il markup generato da `lib/markdown.ts` (immagini nel corpo del post, card di anteprima link) resta HTML grezzo via `dangerouslySetInnerHTML`, non un componente React: `next/image` non è applicabile lì senza riscrivere la pipeline di rendering Markdown; aggiunti però `loading="lazy"`/`decoding="async"` diretti sugli `<img>` generati, stesso beneficio di lazy-loading senza quel refactor. `LinkPreviewCard.tsx` (editor, non pagina pubblica) non toccato. |
| `RichTextEditor` (TipTap) caricato con `next/dynamic` | ✅ | L'editor (StarterKit + estensioni tabella/immagine/link/markdown) è il bundle più pesante delle pagine di dashboard/admin che lo montano. `frontend/src/components/editor/RichTextEditorLazy.tsx` lo carica con `dynamic(..., { ssr: false })` e uno skeleton di caricamento; i 6 punti che lo importavano puntano ora al wrapper invece che al modulo diretto. Nessuna pagina pubblica lo monta. |

## Come usare questo documento

- Prima di iniziare un nuovo blocco di lavoro, controlla qui se è già presente
  (anche parzialmente) per evitare di duplicare cose fatte.
- Quando un task passa da 🟡/⚪ a ✅ (o viceversa, se si scopre una regressione),
  aggiorna la riga corrispondente nella stessa modifica che tocca il codice.
- Le righe 🟡 e ⚪ sono, nell'insieme, il backlog: per proporre un prossimo
  passo, si parte da lì.
